# Retired Approaches (why they died — don't resurrect without new evidence)

- **Hierarchical two-level solve** (`--hierarchical`, level-1 foot tasks / level-2 body+hand): REGRESSED pivoting get-ups. Root failure: promoting the reach-limited palm pin to hard starved body tracking. Flag retired, OFF. Shipped path = single-level all-soft weights. **UPDATE (hierarchical-v1 H2, 2026-07-11)**: re-tested narrower — `--hierarchical`/`--hard-tier` with ONLY feet hard (hands left soft, matching the original diagnosis) is SAFE, verified on `standup_natural_01` (the clip the original regression named): `mean_err` stays in the same range as the non-hierarchical baseline. This narrower form is now available (`--hard-tier`, default off) and not itself retired — only the full (feet+hands) form remains dead.
- **Floor non-penetration as a hard (level-1) tier in Stage 3** (`--floor-hard`, hierarchical-v1 H2, 2026-07-11): mixing `floor_collision_rows` into the same level-1 system as foot-hold position-equality rows caused a 44-METRE divergence on `standup_natural_01` (`mean_err` 0.05m → 44.4m over the clip) — isolated via direct A/B against `--hard-tier` alone (feet-only-hard), which stayed safe. Generalizes the fullmesh-collision-infeasibility lesson below from Stage 4's OSQP inequality QP to Stage 3's nullspace equality-row architecture: an inequality-style avoidance term (push away by up to N cm if penetrating) does not mix safely into an equality-style hard tier (exact position pin) — when a held anchor carries residual floor penetration, the two hard rows directly conflict every re-linearization, compounding instead of damping. Flag kept in the codebase (default off, help text marked "CONFIRMED BROKEN — do not use without a redesign"), not deleted. Full numbers: `planLog.md` H2.
- **Hard-equality foot pins in Stage B**: never actually shipped (docstring lies — see [[globalopt]]); the hard COLLISION path did ship and died on fullmesh infeasibility (see [[fullmesh-vs-primitive]]).
- **Upright-root constraint**: makes limbs absorb the whole lying-to-standing rotation on get-ups, picks bad IK branches. Root orientation left free.
- **Per-frame velocity cap in-solver**: had a collision interaction problem, unresolved — superseded by Stage A global smoothing.
- **Stage B all-off rule** (old): artifact of loose contact detection; superseded once hysteresis + foot-hold ×10 made plants stationary.
- **Legacy worlddelta solver family** (`scripts/legacy/`: `solve_fbx_canonical_alex_posori_qp_fresh*.py`, bodypos variants, MVNX path, old renders): the pre-contact-first pipeline, baseline tag `baseline-posori-worlddelta-v1`, branch `initialBaseline`. Kept for reference, not run. Its contact-labels exporter `post_process_grounding_contacts.py` is the template for the future Mimic export (see [[open-questions]]).
- **Swing-foot toe-clearance de-pitch in Stage 3** (`--swing-clear`, 2026-07-13): to kill the get-up-class swing-foot floor dig (rigid foot plate toe digs ~10–18 cm through the floor when copying the human's plantarflexed low-step, see [[tradeoffs-limits]]), added (a) a cap on the toe-down pitch of a swing foot's orientation target, (b) a paired hip/knee temporal-continuity `posture_reg` boost to stop the redundant-leg IK branch-flipping (an aggressive cap alone flips → 43°/frame jump Stage-4 can't smooth), (c) a self-collision weight boost to stop the continuity reg jamming thigh↔torso. Corpus: `anyPen` mean 9.0→5.6 cm, **0 spikes, 0 JLvi, self-pen depth ~flat** (0.85→0.91 cm) — looked like a win on the numbers. **REJECTED on visual review**: on `luigi_standProne_03` the aggressive de-pitch **contorts the leg** — flattening the swing foot while the leg is tucked *under the body* (not stepping) forces a grotesque knee/ankle pose (smooth in joint-velocity, ugly in pose; scalar smoothness metrics missed it entirely — had to render frames to see it). Root insight: **this is not a stage problem** — flattening a *tucked* foot is intrinsically ugly regardless of Stage 3 vs Stage 4, so the mooted "do it in GlobalOPT instead" (Probe B) would hit the same contortion; the issue is kinematics, not smoothness. The get-up swing "penetration" is largely **tucked feet curled under the body** (an embodiment-gap pose, [[tradeoffs-limits]] knee-140° limit), not a swinging step that can be lifted. Code kept, all flags **default off** (`SWING_CLEAR=off`; shipped pipeline byte-unchanged); full knobs/sweep in `planLog.md` swing-clear. Don't re-tune with orientation caps — needs coordinated whole-foot re-placement or nothing.
- **`--leg-floor-refine` (Stage 3, 2026-07-13)**: built to do "coordinated whole-foot re-placement" (synthesize a temporary plant for a penetrating leg + relax pelvis/torso tracking so root can rise) — Prabin's proposal after `--swing-clear`'s rejection. NOT retired/wrong, but found INERT on its target case (`luigi_standProne_03`): detected zero penetration in Stage 3's own raw output, because that clip's dig is actually introduced downstream by Stage 4 (see [[globalopt]]'s floor-sensitive-frames section) — Stage 3 was never the problem there. Superseded for that failure class by `--sens-foot-min-pen` (Stage 4). Kept in the code (default off) — may still be the right lever for a genuinely Stage-3-originating case (e.g. `luigi_standSupine_08`'s knee-140° class, [[tradeoffs-limits]]), untested standalone since the two mechanisms weren't cleanly separated until after this was built.
- **Branch `feature/fbx-kinematic-canonical-v2`**: parallel solver with different segment assumptions — kept for exploration, not retired-dead but not active.
