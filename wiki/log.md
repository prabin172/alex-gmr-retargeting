# Wiki Log

Append-only. Format: `## [YYYY-MM-DD] <op> | <what>` — parseable via `grep "^## \[" log.md | tail -5`.

## [2026-07-18] kill-test | Z-shift oracle INVALIDATES the S3 held-frame framing
Prabin's objection ("just move the robot down") tested directly: per-clip constant downward shift
of GMR-polished (oracle dz via grid sweep; rigid shift is exact-analytic for these metrics) beats
OURS-ctground on held-frame frac3 (96-99% vs 82-87%) AND max floorPen (6.6-13.4cm vs 17-23cm) on
both classes; 76/77 clips ≥80% frac3 vs OURS's 50/77. Mechanism: GMR's held float is near-constant
within a clip (p90-p10 ≈ 2.6-3.3cm), so one shift zeroes it. Also established: OURS's pen% (62-81%)
starts at the RAW solve, not grounding. Honest bar going forward = simultaneous contact + no-pen
(joint per-frame metric), vs the oracle-shifted baseline — nothing currently meets it. CSV:
`outputs/gmr_baseline/sprint/s3_zshift_oracle.csv`. Details: sprint-s2 page, new kill-test section.

## [2026-07-18] eval | Sprint S3 full-corpus (77 clips) build + eval complete
Build (`sprint_s3_full_corpus.py --build`, resumable) finished 77/77, 0 failures; eval
(`--eval`, combined GMR+OURS CSV, 462 rows) confirms the 4-clip held-frame-contact finding at
corpus scale: OURS ~0cm held-foot median / 82-87% within 3cm vs GMR-polished 4.4-11.7cm median /
0.3-31% within 3cm, on both floor (n=34) and locomotion (n=43) classes. Aggregate floorPen still
trails GMR-polished (known reach-limit + warm-start-basin mechanism, `knee_bias` OFF). New
`scripts/g1/sprint_s3_summary.py` (class-split table). Details:
`wiki/experiments/gmr-baseline-sprint-s2.md`'s new "Full-corpus result (S3)" section.

## [2026-07-03] init | Wiki created
Seeded from METHOD.md, SESSION_HANDOFF.md, README.md, paper_idea.md, paperIdea2.md, repo scan.
18 pages: concepts (9), data (2), experiments (3), results (2), questions (2). CLAUDE.md slimmed to
session protocol + critical conventions; details moved here.

## [2026-07-03] ingest | Undermind lit report → paperIdea3.md (Humanoids 2027)
Read the 38-page Undermind deep-research report (170 refs): niche confirmed unoccupied, 4 concrete
gaps (floor-stance templates, contact sequencing, knee/pelvis target editing, kinematic hardware
playback). Consolidated paper_idea.md + paperIdea2.md into paperIdea3.md — "Any-Contact Retargeting"
full paper for Humanoids 2027 (C1 generalized contacts, C2 template library, C3 quasi-static CoM,
C4 hardware playback; 12-month plan, BeyondMimic gate Oct 2026). New wiki page concepts/related-work;
questions/publication rewritten as two tracks.

## [2026-07-03] lint | Old idea docs deleted by Prabin
`LLM Wiki.md`, `paper_idea.md` (untracked, gone), `paperIdea2.md` (tracked — recover via
`git show HEAD:paperIdea2.md`). Content absorbed into paperIdea3.md + wiki. Dead refs fixed in
CLAUDE.md, README.md, questions/publication.md.

## [2026-07-03] write | paper_intro.md drafted + SESSION_HANDOFF rewritten lean
Introduction v0.1 for the 2027 Any-Contact paper (intro→background→lit→gap→contributions, [TBD-*]
placeholders, revision notes). Handoff slimmed to session-delta + uncommitted state + NEXT; wiki is
now the technical record. Next session: fullURDF render inspection, C1 (generalized contacts),
mentor items.

## [2026-07-03] fix | IHMC export fps bug: src_fps now real-time (fps/stride=30), not capture 120. Regenerated all 18 outputs/ihmcJsons-120hz/ at correct 4x duration.

## [2026-07-05] analyze | rsl_rl consumer traced: IHMC RL tracker consumes at 50 Hz (json_to_npz --output_fps 50, ZOH no-interp; replay dt=0.02; all ref motions fps=50). 30 Hz solve is sub-Nyquist → self-upsample 30→120 launders lost content. Plan: solve at ≥50 Hz (STRIDE=1→120 or 2→60), downsample only at render/consumer. Watch LAMBDA_SMOOTH dt-retune. Wiki ihmc-export + pipeline updated.

## [2026-07-05] change | Native 120 Hz solve: STRIDE 4→1, LAMBDA_SMOOTH 20→320 (×16 ∝fps²), GROUND_SMOOTH 5→80, N_OUTER 3→6, contact min-run/ramp/preroll ×4. Fixed OSQP status bug ("solved inaccurate" space vs underscore) + max_iter 8000→20000. Validated on 3 clips vs archived 30 Hz baseline (scaling correct; standup ~9cm slip pre-existing; get-up coll needs n_outer=6). Full 18-clip batch run; 30 Hz baseline archived outputs/*_30hz/. Review next session.

## [2026-07-05] diagnose+fix | SCA parity bug → keep-best-iterate (slip-aware). Reviewing the 120 Hz batch found the get-up self-collision "regression" was NOT frame rate and NOT OSQP: the SCA loop re-linearizes collision only at each outer's start, so a collision-free outer drops all collision rows and takes an unconstrained step back into ~6 cm penetration → per-outer penetration OSCILLATES, and returning the last iterate makes the result depend on n_outer PARITY (30 Hz n=3 odd=lucky, 120 Hz n=6 even=bad). Verbose re-solve of standup_side_04 proved it (outers 3&5 hit 0.00 cm, discarded; outer 6 shipped 6.59 cm). Fix: stage_b returns the best iterate across outers, scored lexicographically (pen-beyond-1cm, then pen+slip, then coll%), seeded with the Stage-A warm start.

## [2026-07-05] tune | Plant-slip reduction. plant-speed sweep (0.05/0.04/0.03) = DEAD lever (slip 10.4→10.9 cm, slow steady drift stays sub-threshold). Pin-weight sweep IS the lever: foot/hand ×4 (40/8→160/32) cut standup_side_04 slip 10.4→6.3 cm for <1 cm shallow collision; ×16 barely beats ×4 (diminishing). Finalized #3 = pins ×4 + slip-aware keep-best. Pipeline: added FOOT_WEIGHT/HAND_WEIGHT knobs + Stage 6 IHMC export (outputs/ihmcJsons-native120hz, native 120 no --fps).

## [2026-07-05] batch | Finalized 18-clip run, ok=18 fail=0 (clip 11 shovel_rightbucket_01 no longer crashes). Peak self-penetration ≤0.88 cm + spikes 0 on ALL 18. Slip outliers: standup_side_05 14.7 cm/38% grazing, kneelingFall_03 10.7 cm — contact-labeling floor, not regression. 18 IHMC JSONs regenerated native 120. Log outputs/logs/pipeline_native120_20260705_141837.log.

## [2026-07-05] doc | METHOD.md updated to shipped reality: header (branch main, single fullmesh model, no _v2/_v2_fullmesh/--soft-collision), Stage-6 export subsection §7.1, §6.2 keep-best+slip-aware selection + pins ×4, new §6.4 native-120 rate-scaling table+derivation, §8 model consolidation, §9 refreshed metrics. Wiki globalopt/pipeline/metrics synced.

## [2026-07-05] diagnose+fix | standup_side_05 slip 14.7cm = PHANTOM. Per-effector dig: entirely right_hand, entirely 25 single-frame "plants" (velocity zero-crossings while the hand lifts off in late standup); IK-vs-median ≤0.4cm (plant IS locally stationary), Stage A smooths hand off the 1-frame anchor. NOT the "structural repositioning floor" I claimed earlier — that take was wrong. Fix: plant_min_run=8 stillness debounce in _compute_anchors (still run <8 fr → moving, not plant) + --plant-min-run CLI + PLANT_MIN_RUN=8 pipeline knob (×4 frame-count, 2@30Hz). Standalone standup_side_05 14.7→6.8cm, coll/spikes/track unchanged. Full 18-clip batch re-running with fix → review next session (watch kneelingFall_03 10.7cm). solver+pipeline uncommitted.

## [2026-07-06] export+pipeline | Added 50 Hz IHMC JSON set. `1.json` (IHMC ref) confirmed ~50 Hz, `1.npz` fps=50 (json→npz keeps rate, NOT 100). Regenerated 18 clips from the native-120 grounded NPZs via exporter `--fps 50` (genuine 120→50 downsample, not upsampled-from-30) → `outputs/ihmcJsons50hz/` (all exactly 50.0 Hz). Wired Stage 6b into retargetingPipeline.sh (EXPORT_50HZ=1 default, IHMC_DIR_50 knob). Old `outputs/ihmcJsons/` (30 Hz-origin) left in place but superseded. Wiki ihmc-export.md updated.

## [2026-07-06] diagnose+fix | RDX playback: foot-off-ground + bobbing. Root cause of the floating foot = Stage-3 TARGETS put the two ankles 5.78 cm apart in Z while both feet contact-labelled (inconsistent input a 1-DOF grounding shift can't reconcile), NOT a solver bug. Bobbing = perframe grounding shift wanders 7–9 cm as the lowest contact migrates hands→knees→feet. Fixes (3 stages): (3) `--coplanar-feet-mode mean` snaps both ankle-height targets to a common Z on both-feet-contact frames (achieved ankle gap 4.66→0.95 cm); (4) sole-corner on-floor rows drive each planted foot's 4 corners to a shared floor_z = on-floor+flat+coplanar in one row type (final sole gap →0.54 cm), pin split to X,Y on planted frames, keep-best score gains foot_floor_err + pen gate→2 cm, plant-slip made horizontal-only for feet; (4.5) new grounding mode `constant-contact` (now default) = single shift keyed to planted-foot soles (median) → 0 bobbing AND feet stay down (plain `constant` floats them +9.8 cm grounding on early hands/knees). Result standup_02: worst foot 4.7→0.6 cm off floor, wander 9→0 cm, peak self-pen 0.5→1.4 cm. Only standup_02 regenerated + re-exported; 18-clip re-run pending. Wiki globalopt/contact-first-ik/grounding updated. L/R-mirror-vs-RDX convention unconfirmed.

## [2026-07-06] batch | Full 18-clip coplanar re-run (COPLANAR_FEET_MODE=mean, FLOOR_WEIGHT=200, GROUND_MODE=constant-contact, RENDER=0). ok=18 fail=0, spikes 0. Peak self-pen ≤1.86cm (kneelingFall_02; up from pre-coplanar ≤0.88 — coplanar/floor rows extend get-up legs ~1cm more grazing); shovels+squat 0.00. Feet planted: 7 standup/get-up clips finish both feet within ≈0.6cm of floor, coplanar pair split ≤0.6cm (standup_02 L0.60/R0.06) — RDX floating-foot gone. Caveat: the 13–16cm Stage-B floor_err on natural/side/slideHandsBack is a max-over-planted-frames artefact from early high-plant frames, NOT final stance. Two FALL clips penetrate the floor plane at the end (kneelingFall_02 Lfoot −11cm, kneelingFall_03 Rfoot −15.8cm): late free foot below the single constant-contact shift — separate regime, wants per-frame/hybrid grounding. 18 native-120 + 18 50Hz JSONs regenerated. METHOD.md §9 measured stats updated. Log outputs/logs/pipeline_coplanar.log.

## [2026-07-07] batch+discuss | Two NEW clips, new subject (Luigi): luigi_standProne_03, luigi_standSupine_08 from data/raw/inhouse/LuigiStand/. Full pipeline run via new `CLIPS_MATCH` name-filter env in retargetingPipeline.sh (+ 2 CLIPS entries) so a subset runs without recomputing the 18. Per-subject scaling confirmed OK: both scale calcs are Euclidean-distance ratios from the clip's own first frame (orientation-invariant → new subject + prone/supine rest both fine, no per-subject config). Prabin's read: kinematics good, but NOT usable for RL yet — feet not fully planted / ground penetration up (the documented constant-contact single-shift tradeoff: soft FLOOR_WEIGHT rows pull-not-pin; a ~2cm hovering foot becomes a phantom target the mimic tracker chases). Decisions: (1) real fix = automated foot-lock post-process (proposed Stage 4.6: per-planted-foot IK snap sole→z=0+flat during plant intervals, hold body, blend at boundaries), NOT manual per-clip Blender; (2) teammates trying manual Blender + floated dropping this pipeline — Prabin refuses; framing = our pipeline is the 90% (morphology/IK/orientation/self-collision/smoothing), foot-lock is the last 10%; (3) if Blender stays it should start FROM our grounded NPZ (already robot qpos), not raw human (would re-owe the whole retarget by hand); (4) prone-standup hands-thrown-forward-for-momentum is a motion-specific tracking problem (would need hand-vs-leg reweighting, one motion type only) — but manual Blender is even more motion-specific (one motion, period). Next: prototype Stage 4.6 foot-lock, test on luigi_standProne_03 + a standup clip. retargetingPipeline.sh CLIPS/CLIPS_MATCH change uncommitted. Detail in SESSION_HANDOFF 2026-07-07.

## [2026-07-06] doc | METHOD.md §6: added in-detail the Stage3→Stage4 data hand-off (qpos_ik = smoothing target + contact-anchor source + Stage-B linearisation origin; target_positions/contact_flags reused; achieved-ori/align-errors/self-coll-counts carried but unused), the mesh→QP-row hand-off ("mesh never enters the QP as geometry" — mj_forward reduces mesh to contact points = dist/normal/pos; one row per contact, j_sep coeffs + pen RHS; QP size scales with #contacts not vertices), and the OSQP assembly mapping (Jacobians→P/q/A, pen→l, slack→P penalty+A identity). Wiki globalopt.md gets a summary block pointing to METHOD. Answering a "which QP / why didn't the huge mesh choke it" question. No code change.

## [2026-07-08] diagnose | Foot-slip vs FOOT_WEIGHT resolved — NO tuning. New tool `scripts/diagnose_foot_slip.py` (reuses solver's `_compute_anchors`/`_contact_point`/penetration filter; per-frame foot XY deviation from frozen anchor vs max inter-limb pen, ±6-fr windowed corr per mimic-repo review). Key: the 6.3cm standup_side_04 slip was STALE (pre coplanar+floor); current config = 1.9cm. Diagnostic on fresh re-run: slip↔pen corr −0.08 (collision-FREE, not the leg-crossing prior); FOOT_WEIGHT sweep 160/1000/4000 → slip 1.9/1.7/1.7cm while pen 1.61/2.16/2.46 + coll 8.8/9.7/12.8% climb. Slip floors at 1.7cm = λ_smooth transition-blend at plant EDGES (fr 495→505 ramp), not a weight deficit or collision floor. Decision: FOOT_WEIGHT stays 160, no Stage 4.6, drop the standupTuned footlock branch. Only open q: is 1.7cm below the mimic tracker's reward std. globalopt.md updated.

## [2026-07-08] eval | New corpus artifact table: `scripts/eval_artifacts_corpus.py` (per-clip joint-limit / contact-slip / self-collision, reuses solver anchors+penetration filter at strict COLL_HOPS=2, evaluated on global_opt = grounded-equivalent). CSV `outputs/artifact_table.csv`. Findings across 20 clips: ZERO hard joint violations; NECK_Y systematically pinned 48-63% on standups (head-ori target over-drives neck pitch — the one actionable limit finding); foot slip median-max 2.1cm / p95 1.0cm, worst is FALLS not standups (kneelingFall_03 8.3cm foot; standup_02's 6.1cm is a HAND push-off); self-collision median 8.3% but all shallow grazing (peak pen ≤1.9cm everywhere, kneeling clips drive coll%). Slip split foot(horizontal)/hand(3D). metrics.md updated with the table + methodology.

## [2026-07-08] eval | Added GROUND-contact metrics to eval_artifacts_corpus.py (reads grounded_contactfirst/, floor=z=0): planted-foot penetration vs any-foot(swing) penetration vs planted-foot float. Big reframe — the RL blocker is DOWNWARD ground penetration, not XY slip or float. (1) Planted-foot pen median 2.5cm / max 6.5cm on nearly every clip incl shovels — root cause = constant-contact registers to planted-sole MEDIAN (--contact-percentile 50), so ~half of planted frames sit below floor by construction; fixable by lowering the percentile (trades vs float). (2) Swing/tucked-foot clipping up to 28cm (standup_side_05; non-contact feet during deep crouch, root z=0.21) — single rigid shift can't fix, needs geometry-aware/hybrid grounding (the known fall/get-up gap; matches handoff kneelingFall -11/-15.8cm). (3) Float tiny (median 0.2cm) — grounding errs down not up, so the feared 'hovering plant phantom target' is really a below-floor foot. metrics.md + grounding.md updated.

## [2026-07-08] diagnose/correct | Get-up floor residual is BETWEEN-PHASE, not within-plant Z-drift (corrected an earlier read). Per-plant-window split: feet frozen 0.2-0.6cm WITHIN each window; multi-cm spread is entirely BETWEEN windows at different postural phases (lying/kneeling root-z~0 vs standing root-z~0.5). Implemented+tested a Z-anchor freeze (restore full-3D body pin in _build_contact so Z gets XY's frozen-median anchor) on Luigi clips -> moved standSupine_08 spread 3.51->2.89cm only (real geometry, not soft-anchor gap) -> REVERTED. Real fix = grounding registration to the TERMINAL standing stance: for get-ups that's a HIGHER contact-percentile (opposite of the earlier p25 direction, which floats the functional stance). luigi_standSupine_08 p50 standing floats +2.56cm -> p70 plants +0.12cm (lying penetrates 3.4cm, fine/non-weight-bearing); p70 keeps luigi_standProne_03 planted (-0.15cm). standProne_03 ground already fine, only residual is 4.2cm XY slip (separate). Principled unbuilt fix: register on highest-root-z plant window. grounding.md updated. notes.md replaced.

## [2026-07-08] diagnose | Foot-frame sole-normal bug (settled). Canonical foot frame sets x=toe-ankle (bone axis, declined ~18deg below horizontal since ankle sits above the grounded toe), z=x*y, so "sole normal" reads ~18deg from vertical for a FLAT foot + ~4deg L/R skew — frame geometry, not motion (constant across 20 clips + both subjects; arctan(4.6/14)=18.2deg matches measured 18.7deg). Impact confined to Stage-3 contact-detection tilt gate (world-delta transfer + robot-side foot-flat enforcement unaffected). Fix = per-foot SELF-CALIBRATED baseline subtraction (gate on tilt - p15 of that foot's planted-frame tilt), NOT projecting x onto horizontal (proven to zero the pitch signal: flat/+10/-10 all read 0deg projected, since toe-ankle vertical component = baseline+pitch summed). No heel marker (ankle/toe/toe_end only) so no two-ground-point sole plane; baseline subtraction is the only pitch-recovering option. Post-calibration corpus re-audit: 258/286 plant windows <3deg above baseline (clean flat), phantoms 6-16deg (luigi_standSupine supine foot 7.6deg = genuinely non-flat). 3-6deg gap = 14 windows -> default LEAVE (don't force). Design converged w/ mimic-Claude: build (1) frame self-calibration first, then (2) confidence-gated per-segment ground enforcement (force z=0 only when tilt-above-baseline<3deg; >=3deg leave). orientation-frames.md updated. Nothing in pipeline changed yet.

## [2026-07-08] BUILD (1) | Baseline-relative flat gate SHIPPED in detect_contacts_from_human (solve_fbx_canonical_alex_contactfirst.py). Was raw `tilt<40deg` (nearly inert given the ~18deg toe-ankle frame offset); now per-foot self-calibrated baseline (p15 of candidate-contact tilt) + gate `tilt-baseline < --foot-flat-margin` (default 6deg), absolute --foot-flat-tilt=40 as <20-candidate fallback. Corpus detection-only check (20 clips, pre-IK so seconds): (a) 4 real-plant frames dropped (1 borderline window), (b) 3 phantom frames leaked, (c) 0/40 feet hit fallback -> self-calib universally applicable. Validated on Luigi: standSupine_08 supine phantom 100%->0% dropped, standing preserved, Stage-4 floor_err 14.3->0.68cm; standProne_03 no regression. NECESSARY-not-sufficient: grounding float persists (+2.56->+2.03cm) because genuinely-FLAT contacts remain at different heights across phases (rise -11.5 vs stand -7.2cm) — that's build (2)'s job (confidence-gated per-segment ground enforcement, force z=0 only when tilt-above-baseline<3deg). contact-first-ik.md + orientation-frames.md updated. Existing outputs/ NPZs still hold OLD labels; take effect on next Stage-3 run.

## [2026-07-08] BUILD (2) | Redirected by measurement: it's a GROUNDING-registration fix, NOT a QP change. Prereq (debounce vs new segment lengths): clear — post-relabel genuine flat plants stay long/contiguous (100-600fr), only 1-4fr spurious blips fall below contact_min_run=12/plant_min_run=8. Key measurement: calibrated Stage-4 solve ALREADY plants clean stances consistently at floor_z (standSupine_08 both stances -6.90/-6.92 = floor_z -6.91, floor_err 0.68cm); the rise-phase low contact is correctly reclassified MOVING by _compute_anchors, QP never enforces it. So no per-segment QP enforcement needed (contradicts mimic-Claude's "keep in QP" premise, which assumed the solve left segments apart — the label fix disproved it). Residual +2cm float was a grounding-registration MISMATCH: constant-contact keyed on RAW contact_flags (incl. moving rise-phase low frames) not the still stances. FIX (shipped in post_process_ground_contactfirst.py _planted_foot_sole_samples): still-filter — foot only anchors registration on frames where its body speed < --still-speed (0.05, = solver plant-speed), per-foot fallback to labelled if no still frames. standSupine_08 standing +2.02->+0.04cm; regression spot-check (shovel/side/fall/standup) <=0.2cm = noop where not needed. Two-phase plan complete: (1) baseline-relative flat labels + (2) still-filtered registration. PENDING: full corpus Stage-3+4+ground re-run (labels changed) + regenerate stale artifact table. grounding.md updated.

## [2026-07-09] diagnose | Step-0 target-space diagnostic corrects the ground-penetration diagnosis: foot = free-swing tracking sag + toe-down dig (targets fine, floor term needed); hand = palm-pin target below floor (clamp needed); prior Stage-3 failure = floor placed 8cm too high. Plan: collisionFixPlan.md; tool: scripts/diagnose_floor_penetration.py

## [2026-07-10] fix | Ground-penetration + wrist-flick RESOLVED for luigi_standProne_03: target-space floor placement (Stage 3) + palm-pin clamp + floor repulsion (no ramp -> wrist limit-slam, fixed via targeted 2-pass arm refinement around floor-onset windows: _detect_arm_floor_onset_windows/refine_arm_floor_transitions) + Stage-4 mesh floor collision. Result 11.5cm->2.4cm pen, 0 spikes (was 0->1 spike mid-fix, now 0). luigi_standSupine_08 explicitly excluded (already clean baseline; hard floor-collision made it worse due to lying/standing multi-phase floor-height conflict, see grounding.md). Per-clip only, not corpus-wide. Deliverables regenerated via real pipeline.

## [2026-07-10] fix | Phase-aware floor collision RESOLVES luigi_standSupine_08 too (follow-up to same-day entry above): floor_phase_weight() (both solver scripts) — smoothstep of pelvis/root height between clip-low and planted-foot/standing reference, gates hard floor-collision rows+counting per frame instead of clip-wide. Fixes the prior session's exclusion reason (false 14.4cm RIGHT_HIP_X_LINK "penetration" during lying phase) AND catches a real, previously-invisible bug: baseline RIGHT_FOOT genuinely clipped 4.4cm through the floor during the stand-up transition (frames 758-772), invisible to eval_artifacts_corpus.py's planted-foot-only check. Cost: small boundary artifact (~5 frames, self-pen to 2.1cm, no spikes) + real slip/flat-error increase (1.6->4.3cm, 0->5.2deg) — accepted trade. Still 0 spikes. No-op for single-phase clips (luigi_standProne_03 outputs confirmed byte-unchanged). Shipped via real pipeline. NOT done: Stage 4.5's grounding percentile is separately phase-blind (grounding.md's p70-for-get-ups recommendation, unbuilt) — used pipeline default p50 for this deliverable, orthogonal follow-up if needed.

## [2026-07-14] diagnose | New `hybrid` Stage 4.5 grounding mode (branch p0-grounding, uncommitted) tested on the two known trouble clips: real improvement but does NOT close the gap. luigi_standSupine_08 -16.3cm->-4.7cm, standup_side_05 -25.9cm->-21.2cm (barely moved). Root cause isolated (not a smoothness-tuning issue, confirmed by --lift-smooth 1e4->10 sweep): the still-plant cap binds tighter than the whole-body need when a DIFFERENT body part is deeply penetrating in the same frame as a near-zero-margin still plant -- same between-phase conflict as the old single-shift design, just moved to per-frame granularity; a scalar lift cannot satisfy two disagreeing body parts in one frame. Safe strict improvement, not a full fix. grounding.md updated.

## [2026-07-14] plan | Continuation/homotopy floor-penetration solve planned -> plan.md (repo root, ACTIVE, written for a lower model to execute). Mechanism: K passes of existing Stage-B, per-pass floor allowance eps_k shrinking from measured pass-0 penetration to 0, floor slack penalty ramping 1000->1e5, tracking relaxed only in violating frame-windows x limbs (trunk excluded, root stays frozen), warm-start chaining, cross-pass keep-best seeded with pass 0 (can never ship worse than today). Gate clips: standup_side_05, luigi_standSupine_08, standup_natural_01. Context: Luigi manually authored a kinematically feasible standup (Slack 2026-07-13) proving feasibility exists outside our warm-start basin; multi-seed and NLP migration considered and rejected (36k-dim random seeds land nowhere; NLP is still local). Time-invariant tracking + dynamics parked (blocked on actuator specs; retiming is a no-op for kinematic penetration).

## [2026-07-14] experiment | continuation-v1 GATED, did NOT clear ship bar (1/3 clips, needed >=2/3). Implemented per-frame floor-allowance homotopy + hardening slack penalty + limb-scoped tracking relaxation in solve_global_trajectory_opt_contactfirst.py (--continuation N, default 0, verified byte-identical no-op). Caught + fixed one design bug en route: first version used a clip-wide scalar allowance sized to the WORST frame, which silently disabled floor rows for every other frame instead of asking each frame for gradual improvement (planLog.md T4). After the fix: luigi_standSupine_08 (the one clip already shipped with --floor-collision on) improved genuinely -- floor pen 3.56->2.68cm, self-pen headroom 0.20->0.01cm, 0 spikes. standup_natural_01/standup_side_05 showed ZERO effect, traced to a DIFFERENT pre-existing problem: their base Stage-B solve never converges below its own warm start under forced --floor-collision on (confirmed via a 20-outer control run, all 20 worse than warm) -- the known SCA-oscillation weakness, just never exercised on these two clips before (floor-collision was never turned on for them). Continuation cannot fix a non-convergent base solve. Not wired into pipeline defaults. Full writeup: wiki/experiments/continuation-v1-gate.md, wiki/concepts/globalopt.md, planLog.md (repo root).


## [2026-07-14] eval | Mentor manual-Blender retargets received (data/blender-retargeted/std{Prone,Supine}.json, IHMC KinematicsToolboxOutputStatus @50Hz, same joint_name_hash as our Stage-6 export -> directly loadable). New tool scripts/eval_ihmc_json.py: reference-free physics eval (mesh-exact floor pen vs z=0, fullmesh self-collision, joint limits, rate-aware velocity, stance metrics from the JSON's own foot flags) + --save-npz for rendering. Head-to-head vs our 50Hz exports of the same clips: HIS WINS supine floor pen (3.0 vs 16.3cm), self-collision (0.0% both clips vs our 16.3%/7.8% incidence), plant metrics (pen 3.0/1.9 vs 14.0/4.2cm; slip 3.3/1.9 vs 18.1/8.6cm raw-flag proxy). OURS WINS smoothness (his supine vMax 25.7 vs our 11.5 rad/s; both 0 spikes by the 60 rad/s convention) and prone worst-case pen (ours 9.5 vs his 14.0cm). KEY NUANCE prone: his 14cm worst pen = LEFT_GRIPPER punch-through during push-up (frame 343, 6.9s) AND his thighs+torso sit >2cm below floor for ~73 percent of frames -- his floor was keyed to the standing feet, i.e. the SAME between-phase lying-floor conflict our pipeline has (grounding.md): manual editing didn't solve it either, it parked the lying body below z=0 where IsaacLab will still see it. Renders: outputs/cont_dev/renders/blender_std{Supine,Prone}.mp4. Caveats: his contact flags are hand-authored (locked feet) vs ours raw resampled labels incl. moving contacts (slip proxy overstates ours); his motion was authored against URDF geometry, judged here by our convex-hull fullmesh.

## [2026-07-14] build+test | "Polish Luigi" warm-start pipeline WORKS (the right-basin idea from the 2026-07-13 Slack thread). New scripts/ihmc_json_to_stage4_npz.py: mentor's manual Blender retarget (IHMC JSON) -> Stage-4-ready NPZ where HIS motion is both warm start and tracking reference (target_positions = FK of his own qpos, contact_flags = his hand-authored foot locks). Ran Stage 4 rate-corrected for 50Hz (lambda_smooth 56 = 20x(50/30)^2, plant_min_run 3), floor-collision+phase-aware on, then Stage 4.5 ground -> re-export 50Hz JSON -> eval_ihmc_json.py, so before/after uses the identical tool. standSupine results (his raw -> StageA-only -> StageA+B): vMax 25.7 -> 4.5 -> 5.0 rad/s (the manual-keyframing roughness, 5.7x smoother), rootV 1.67 -> 0.72 m/s, floorPen 3.0 -> 2.8 -> 2.5cm, plant pen 3.0 -> 2.7 -> 2.2cm, self-collision 0 throughout, joint-limit viol 0. COST: plant slip 3.3 -> 4.4 (A) -> 9.9cm (A+B) -- Stage B re-anchors plants and moved his locked feet; Stage-A-only is the better deliverable here (his foot locks ARE ground truth; B's anchor machinery fights them for marginal pen gain). Deliverables: outputs/cont_dev/blender_standSupine_stageA{.json,_grounded.npz}, renders blender_standSupine_{stageA,polished}.mp4. Division of labor now demonstrated with numbers: his edit supplies the feasible strategy, Stage A supplies the smoothness manual keyframing can't -- at 3.3->4.4cm slip cost. NOT done: standProne (his version has the same between-phase lying-below-floor issue we have -- polish can't fix a below-floor reference, see previous entry), corpus-wide anything.

## [2026-07-15] plan | Feasibility-First-v1 planned -> plan.md rewritten (replaces gated continuation-v1; that plan's record lives in planLog.md + wiki/experiments/continuation-v1-gate.md). Inversion of the current architecture: plan feasibility globally FIRST (per-frame, where 35-dim pose space makes adaptive relaxation + phase-boundary restarts able to cross strategy basins), then optimize similarity (existing Stage 4 + the already-validated continuation, now operating in its healthy-basin case). Human's contact schedule kept (Stage 2.5 labels); Stage-3 wrapper `--feasibility-first` (default off, byte-identical): retry loop on frames failing a floor/self-pen gate -- tracking relaxed 0.5^a on violating limbs, floor/coll weights boosted 2^a, seeded noise restarts at phase boundaries only, q_ref=previous frame for continuity (the refine_arm_floor_transitions lesson). Also replaced notes.md raw reward-QP sketch with the scoping decomposition (retargeting vs strategy-gap vs polish; reward-shaped kinematic QP = mimic without physics, rejected). Gate: standup_natural_01 + standup_side_05, bar = post-4.5 floor pen <=50 percent of baseline, 0 spikes, no visual contortion; partial result still yields a per-frame infeasibility certificate per clip.

## [2026-07-15] build+diagnose | Feasibility-first-v1 PAUSED mid-T3 (branch p0-grounding). Built the per-frame retry wrapper in solve_fbx_canonical_alex_contactfirst.py (--feasibility-first, phase-boundary detection, feasibility probe, retry loop) -- T1/T2 pass (baseline logged: 22-25cm floor pen near-universal on standup_natural_01/standup_side_05 at Stage 3; --feasibility-first off verified byte-identical). T3 (turning it on) diverges catastrophically (floor pen reaches 2-3 METRES). Root-caused: (1) the retry's weight-boost mechanism triggers a genuine PRE-EXISTING Stage-3 DLS instability -- plain Stage 3 with --floor-weight at just 1.5-2x the shipped default of 10 diverges identically with zero retry code involved, a new finding about the shipped pipeline independent of this plan; (2) removing the boost (keeping only tracking relaxation + phase-boundary noise) still diverges; (3) diagnosed why: the lexicographic keep-best score (hard_gate, pen_depth, tracking_error) almost never reaches its tracking-error tiebreak since pen_depth almost never ties exactly between two solved poses, so a retry that marginally improves pen but produces a nonsensical pose still wins and compounds frame-to-frame; (4) a tracking-error sanity cap fix still diverges (plateaus instead of growing) because its reference (that frame's own attempt-0 error) can itself already be corrupted by an earlier bad frame. Proposed fix 3 (untested): cap on joint-space displacement from the incoming warm start instead of task-space tracking error, reusing the existing 0.5 rad/frame spike convention. Paused here per Prabin's call rather than trying fix 3 immediately -- worth reconsidering whether the retry mechanism itself fights the DLS solver's warm-start sensitivity. Full trail: planLog.md "Feasibility-First-v1" section. plan.md header + SESSION_HANDOFF.md updated.

## [2026-07-15] plan | GMR-baseline.md written (repo root): paper-shape plan answering "Alex work is one-robot-centric." Three options: A = robot-agnostic polish-module paper (Stage A + contact QP + grounding as post-processor for any source, GMR outputs included; validated seed = Luigi polish 25.7->4.5 rad/s), B = feasibility-taxonomy/benchmark from notes.md three-way split (folds into A), C = paperIdea3 Any-Contact 2027 re-platformed onto G1. Key unlock: G1 as primary eval platform makes BeyondMimic native and dissolves paperIdea3's riskiest dependency (BeyondMimic-on-Alex). De-risk ladder E1-E5 with stop-loss. Vendored general_motion_retargeting/ package found source-stripped -- baseline runs need fresh upstream clone.

## [2026-07-15] research | GMR-baseline.md sec-6 appended: feasibility of running/replicating GMR + porting our polish/contact-first stages. Verified against local clone ../GMR (YanjieZe/GMR, MIT, mink-based IK): all code/data public (LAFAN1 free, AMASS registration, G1 model + custom collision URDF ship in-repo), conventions match (wxyz, MuJoCo, 7+29 qpos same as Alex), BeyondMimic pipe pre-built (batch_gmr_pkl_to_csv.py). Effort ladder: polish demo days, full polish w/ contact QP 1-2 wks, full contact-first pipeline on their data 3-5 wks (unlock: GMR data_loader.py output IS our canonical-human shape), policy eval adds GPU weeks. Found our own FBX-to-canonical bridge already half-built untracked in the clone (build_fbx_kinematic_canonical_v2.py). Open risk: their exact LAFAN1 eval-subset clip list not confirmed downloadable.

## [2026-07-15] plan | GMR-baseline-plan.md written (repo root): week-1 task-by-task execution plan for E1+E2+E3 (GMR-baseline.md's de-risk ladder), T0-T10, task-level detail for a cheaper model. Repo strategy: new branch `gmr-baseline` off p0-grounding, all new code under scripts/g1/, GMR clone stays untouched (imported as installed package). Decided to use LAFAN1's OWN floor-contact clips (fallAndGetUp/ground families) for E1 instead of bridging our FBX clips — zero ingest work, stronger motivation figure ("fails on its own benchmark"). FBX bridge deferred to week 2+.

## [2026-07-15] execute | GMR-baseline-plan T0-T10 all done, same session, driven task-by-task (no subagent delegation, per Prabin's explicit instruction). Full trail: planLogGMR.md (repo root); results writeup for outside readers: GMR-baseline-results.md (repo root); wiki page: [[gmr-baseline-week1]]. **Verdict: Option A kill-test clears cleanly** — Stage A smoothing + Z-grounding, ported to Unitree G1 with ZERO core-solver-logic changes (both `stage_a()` and `evaluate()` imported unmodified; only genuinely robot-specific inputs, q_lo/q_hi and the model path, were already function arguments), improves BOTH floor penetration (raw 12.9-15.9cm -> polished 1.1-4.0cm on the 3 floor clips, 65-91% reduction) AND peak joint velocity (4.3-7.7x smoother, matching the Luigi/Alex validation's 5.7x) on every one of 5 clips including controls, no cherry-picking. Two footguns hit + solved: GMR's RobotMotionViewer hard-requires a GLFW display even for --record_video (worked around with an EGL-offscreen headless driver, scripts/g1/gmr_headless_retarget.py); this repo's own empty leftover general_motion_retargeting/ dir silently shadows the real pip-installed GMR package if repo root ever lands on sys.path (rule: always insert scripts/, never bare repo root). Honest caveat: polish fixes clip-level floor calibration + smoothness, NOT per-limb physical plausibility — the same splayed-limb pose from the raw motivation figure is still visually present after polish; that's Stage B's job (contact-anchored QP), deferred to E4. Self-collision numbers on G1 flagged as noise this week (18.2% on a clean walk clip — unvetted collision pairs). New code: scripts/g1/{gmr_headless_retarget,eval_motion,load_gmr_pkl,polish_gmr_pkl,render_gmr_pkl}.py, all uncommitted on branch `gmr-baseline`.

## [2026-07-15] plan | Post-E4 course correction: RetargetMatters.pdf cross-read + Week-2 plan authored. Diagnosed why E4 flat-lined on floor clips (feet-only gait detector + detection on the corrupted ROBOT output; the fix is human-side multi-surface labels — pelvis/torso/hands/knees from the BVH — transferred via role map, with anchors that PULL support bodies to z=0 rather than hold them in place). Paper cross-read yielded: (1) integrity item — the paper's clip-global min-height fix is hard-coded OFF in their shipped code (bvh_to_robot_dataset.py HEIGHT_ADJUST=False) and body-origin/mesh-blind when on, so our E1 = their shipped default, but a "GMR+heightfix" baseline column + a floating metric are needed for reviewer-proofing (E1b); (2) their Table I resolves the 21-motion eval list by name (BVH mapping still open); (3) their three named critical artifacts (ground pen, self-intersection, sudden joint jumps) are exactly our eval/polish axes; (4) their §VI future work names multi-robot + environment-interaction — verbatim our lane; (5) surgical E5 target found: their own Dance 5 GMR failure (sudden waist jumps, success 92.75 sim/51 sim2sim) is precisely what Stage A removes. Prabin's directive folded in: two-axis comparison strategy (match/beat on THEIR turf with THEIR metrics, then floor-class wins on top) — GMR-baseline.md §7 (new), GMR-baseline-plan.md Week-2 (W2-T1..T8 + Week-3 outline).

## [2026-07-15] execute | GMR-baseline Week 2 (W2-T1..T7) executed task-by-task per GMR-baseline-plan.md's Week-2 section, following the post-E4/paper-cross-read course correction. Full trail: planLogGMR.md. Wiki: [[gmr-baseline-week2]]. Summary: W2-T1 (fair-baseline addendum) replicated GMR's own paper-described height fix as a separate baseline column without touching the GMR clone — real penetration reduction but trades into 94-99.9% floating on every floor clip, strengthening not weakening the motivation figure; also found our own week-1 polish shares the same floating signature. W2-T2 independently confirmed E4's slip-reduction claim's direction (magnitude differs by methodology, as expected). W2-T3/T4/T5 (E4b: human-side multi-surface contact + pull-to-floor anchoring) — kill-test #1 (contact labels) passed cleanly with cross-validation against an independent robot-side measurement; kill-test #2 (the actual anchoring) hit CHECKPOINT M3, a clear negative: anchors engage and move support points the right direction locally but the whole-clip aggregate REGRESSES floorPen on 4/5 clips (root frozen, trust-region too conservative to close a 5-13cm gap) — visual confirms poses are unchanged. Conclusion: this motion class needs contact-first SOLVING, not post-hoc anchoring; a Week-3+ scope decision, reported rather than pushed further. W2-T6 (self-collision vetting) passed — GMR's own g1_custom_collision_29dof.urdf loads directly via MuJoCo's URDF importer with matching joint order, giving 0.2% self-collision on a clean walk (vs 18.2% mesh-noise). W2-T7 (contact-aware grounding) negative — both new modes dramatically underperform the already-shipped constant mode; no change to week-1 defaults.

## [2026-07-16] execute | Sprint S1 (full-corpus kinematic sweep) DONE, model switched to Sonnet mid-sprint per standing model-delegation rule. Batch-extended week-1/2's pipeline to all 77 LAFAN1 clips × 3 variants (raw/GMR+heightfix/polished): 231/231 eval rows, 0 failures across all 3 batches (retarget/polish/eval), regression-gated exact against the 5 original week-1/2 clips (no drift). One bug caught mid-run and fixed cleanly: a batch script globbed its clip list from the pkl output dir, which also held stale week-1/2 leftover files with colliding names — fixed by sourcing from the BVH directory instead, no real clip corrupted. Added two new metrics at corpus scale: self-collision via W2-T6's vetted model (not the unvetted default), and a faithfulness guard (robot FK vs GMR's own scaled-human target, using the position-weighted `ik_match_table2` correspondence, confirmed by reading GMR's own IK code) — polish doesn't wander. **Corrected the class-split methodology mid-sprint**: an initial hip-Z-p5<0.3 split (week-1's own convention, applied at scale) undercounted floor-contact clips by 14 — hip height alone misses brief non-hip contact (a hand/knee touching down during a stumble while the pelvis stays up), the exact blind spot W2-T3 already diagnosed. Reclassified using W2-T3's own multi-surface human-contact detector (unchanged, run over all 77 clips, floor-class = any non-foot landmark with a sustained ≥1s contact run) → 34 floor-class / 43 locomotion-class, a materially cleaner split with no contamination in either direction. Table-I→BVH mapping checked (paper website via WebFetch + GMR repo configs) and confirmed absent — unmapped, author-contact fallback is Prabin's call, not actioned. Full trail: planLogGMR.md `## S1-T1..T4` + addendum. Results: GMR-baseline-results.md's new "Sprint S1"/"Sprint S2" sections. Wiki: [[gmr-baseline-sprint-s1]]. S2 (OURS on G1) explicitly untouched this pass — still parked at its own CHECKPOINT M4 from the prior session (contact-frame grounding now genuinely works, verified by independent support_z audit, but whole-clip aggregate floorPen still trails GMR-polished on the hardest fall clip because ballistic/no-contact frames have no signal to correct against — a real finding about contact-anchoring's coverage limits, not a bug). New code: scripts/g1/{sprint_polish_batch.sh,sprint_eval_batch.py,sprint_s1t4_summary.py,sprint_reclassify_contacts.py}, all uncommitted on branch `gmr-baseline`.

## [2026-07-16] correct | Two presentation corrections (Prabin's challenges, both valid) folded into GMR-baseline-results.md + standing rules in GMR-baseline-plan.md: (1) the kinematic guard formerly logged as "faithfulness" is renamed source-target deviation (srcDev) — GMR's paper reserves "faithfulness" for its N=20 human-rater user study, which ours is not; (2) float% is a diagnostic, not a scoreboard — all global Z-calibration methods (their heightfix, our grounding) saturate at 91-99% by construction, so few-point differences are never method comparisons; the genuine-contact discriminator is held-frame support-point distance (+13cm GMR-polished vs -3cm contact-in-the-solve). Also confirmed + recorded a measurement bug in the 2026-07-15 chat-reported vetted-self-collision numbers (floor contacts leaked via floor_gid=None on the combined model — raw OURS walk1 is 42.5%/5.1cm, not 99.5%/75cm; polished 30.0%/9.46cm uncontaminated, elbow-in-torso bug is real) — correction task N1 + BeyondMimic prep N2 queued for the executor in GMR-baseline-plan.md "NEXT SESSION".
## [2026-07-16] fix | G1 leg-chain scaling: replaced independent per-role scales (hip=2.45 impossible chain) with GMR's own grouped constants (0.9/0.75); self-coll improved all 4 clips (walk1 15.3->2.1%), held contact intact; swing-frame tracking residual now dominant (planLogGMR.md S2-T7)
## [2026-07-17] fix+diagnose | S2-T8/T9/T10: 4-clip full comparison confirms OURS beats GMR+heightfix decisively on held-frame contact (all clips, not just walk1); found+fixed Stage A floor-blind smoothing overshoot (extended to cover self-collision too, per Prabin's ask); root-caused remaining whole-clip floorPen residual to G1's ~64%-of-human kinematic reach limit (some poses genuinely unreachable), a size-correction fix attempt (matching GMR's two-term formula) was tried, measured, and REJECTED — closed the over-reach but cramped the whole gait and regressed self-collision/floorPen everywhere else. Reverted cleanly, verified byte-identical to the validated S2-T9 state. Full trail: planLogGMR.md `## S2-T8..T10`. Wiki: [[gmr-baseline-sprint-s2]] (new page).
## [2026-07-18] diagnose+fix | S2-T11/T12: built a "hold-tier leak" diagnostic to test whether holding a stance foot drags the swing foot through the floor via shared root DOFs (Prabin's question) — REFUTED (hold_pos_roles empty at all 4 worst walk1 frames; self-collision-weight also refuted, replicating S2-T7's own sweep). Real cause found instead, and it took a second pass to get right — Prabin's pushback ("why would a too-short leg overshoot THROUGH the floor instead of falling short") caught a hole in the first explanation. Confirmed: the knee sits pinned at its hard lower limit (straight) on the bad frames, but it's a warm-start LOCAL-MINIMUM the per-iteration clamp can't climb out of, not a hard reach wall — manually pre-bending the knee in the warm start resolved 2/4 frames almost completely (one clip: 9.76cm pen -> 0.00cm) using the SAME target. Wired the Alex pipeline's existing (never-ported) `knee_bias` DLS regularizer into the G1 script as an opt-in flag. 4-clip result: genuinely mixed (2 clips clearly better, 1 clearly worse on the exact metric this targets, 1 inert) — not shipped as default. Also surfaced a standing property of this solver worth remembering: it is NOT bit-reproducible run-to-run even with zero code changes (redundant-DOF/warm-start gauge freedom) — aggregate metrics stay within ~1-2cm/1pt across reruns, but exact qpos does not; use metric-level comparisons, not bit-identical diffs, when verifying no-ops on this file. Full trail: planLogGMR.md `## S2-T11/T12`.
## [2026-07-18] decide+execute | Prabin: committing to writing a paper on this line, contact-first solving (OURS) stays core (not reduced to polish-only), not giving up. New `scripts/g1/sprint_s3_full_corpus.py` (resumable): OURS's full canonical_human->Stage3->StageA->ctground pipeline extended to all 77 LAFAN1 clips (plain S2-T9 config, knee_bias OFF pending T12's ship decision), evaluated alongside GMR's already-complete S1 77-clip sweep into one combined CSV. Smoke-tested, launched, running detached from the terminal (`nohup setsid ... & disown`) so it survives logout — not yet complete or evaluated.
