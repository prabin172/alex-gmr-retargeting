# Wiki Log

Append-only. Format: `## [YYYY-MM-DD] <op> | <what>` — parseable via `grep "^## \[" log.md | tail -5`.

## [2026-07-03] init | Wiki created
Seeded from METHOD.md, SESSION_HANDOFF.md, README.md, paper_idea.md, paperIdea2.md, repo scan.
18 pages: concepts (9), data (2), experiments (3), results (2), questions (2). CLAUDE.md slimmed to
session protocol + critical conventions; details moved here.

## [2026-07-03] ingest | Undermind lit report → paperIdea3.md (Humanoids 2027)
Read the 38-page Undermind deep-research report (170 refs): niche confirmed unoccupied, 4 concrete
gaps (floor-stance templates, contact sequencing, knee/pelvis target editing, kinematic hardware
playback). Consolidated paper_idea.md + paperIdea2.md into paperIdea3.md — "Any-Contact Retargeting"
full paper for Humanoids 2027 (C1 generalized contacts, C2 template library, C3 quasi-static CoM,
C4 hardware playback; 12-month plan, BeyondMimic gate Oct 2026). New wiki page concepts/related-work;
questions/publication rewritten as two tracks.

## [2026-07-03] lint | Old idea docs deleted by Prabin
`LLM Wiki.md`, `paper_idea.md` (untracked, gone), `paperIdea2.md` (tracked — recover via
`git show HEAD:paperIdea2.md`). Content absorbed into paperIdea3.md + wiki. Dead refs fixed in
CLAUDE.md, README.md, questions/publication.md.

## [2026-07-03] write | paper_intro.md drafted + SESSION_HANDOFF rewritten lean
Introduction v0.1 for the 2027 Any-Contact paper (intro→background→lit→gap→contributions, [TBD-*]
placeholders, revision notes). Handoff slimmed to session-delta + uncommitted state + NEXT; wiki is
now the technical record. Next session: fullURDF render inspection, C1 (generalized contacts),
mentor items.

## [2026-07-03] fix | IHMC export fps bug: src_fps now real-time (fps/stride=30), not capture 120. Regenerated all 18 outputs/ihmcJsons-120hz/ at correct 4x duration.

## [2026-07-05] analyze | rsl_rl consumer traced: IHMC RL tracker consumes at 50 Hz (json_to_npz --output_fps 50, ZOH no-interp; replay dt=0.02; all ref motions fps=50). 30 Hz solve is sub-Nyquist → self-upsample 30→120 launders lost content. Plan: solve at ≥50 Hz (STRIDE=1→120 or 2→60), downsample only at render/consumer. Watch LAMBDA_SMOOTH dt-retune. Wiki ihmc-export + pipeline updated.

## [2026-07-05] change | Native 120 Hz solve: STRIDE 4→1, LAMBDA_SMOOTH 20→320 (×16 ∝fps²), GROUND_SMOOTH 5→80, N_OUTER 3→6, contact min-run/ramp/preroll ×4. Fixed OSQP status bug ("solved inaccurate" space vs underscore) + max_iter 8000→20000. Validated on 3 clips vs archived 30 Hz baseline (scaling correct; standup ~9cm slip pre-existing; get-up coll needs n_outer=6). Full 18-clip batch run; 30 Hz baseline archived outputs/*_30hz/. Review next session.

## [2026-07-05] diagnose+fix | SCA parity bug → keep-best-iterate (slip-aware). Reviewing the 120 Hz batch found the get-up self-collision "regression" was NOT frame rate and NOT OSQP: the SCA loop re-linearizes collision only at each outer's start, so a collision-free outer drops all collision rows and takes an unconstrained step back into ~6 cm penetration → per-outer penetration OSCILLATES, and returning the last iterate makes the result depend on n_outer PARITY (30 Hz n=3 odd=lucky, 120 Hz n=6 even=bad). Verbose re-solve of standup_side_04 proved it (outers 3&5 hit 0.00 cm, discarded; outer 6 shipped 6.59 cm). Fix: stage_b returns the best iterate across outers, scored lexicographically (pen-beyond-1cm, then pen+slip, then coll%), seeded with the Stage-A warm start.

## [2026-07-05] tune | Plant-slip reduction. plant-speed sweep (0.05/0.04/0.03) = DEAD lever (slip 10.4→10.9 cm, slow steady drift stays sub-threshold). Pin-weight sweep IS the lever: foot/hand ×4 (40/8→160/32) cut standup_side_04 slip 10.4→6.3 cm for <1 cm shallow collision; ×16 barely beats ×4 (diminishing). Finalized #3 = pins ×4 + slip-aware keep-best. Pipeline: added FOOT_WEIGHT/HAND_WEIGHT knobs + Stage 6 IHMC export (outputs/ihmcJsons-native120hz, native 120 no --fps).

## [2026-07-05] batch | Finalized 18-clip run, ok=18 fail=0 (clip 11 shovel_rightbucket_01 no longer crashes). Peak self-penetration ≤0.88 cm + spikes 0 on ALL 18. Slip outliers: standup_side_05 14.7 cm/38% grazing, kneelingFall_03 10.7 cm — contact-labeling floor, not regression. 18 IHMC JSONs regenerated native 120. Log outputs/logs/pipeline_native120_20260705_141837.log.

## [2026-07-05] doc | METHOD.md updated to shipped reality: header (branch main, single fullmesh model, no _v2/_v2_fullmesh/--soft-collision), Stage-6 export subsection §7.1, §6.2 keep-best+slip-aware selection + pins ×4, new §6.4 native-120 rate-scaling table+derivation, §8 model consolidation, §9 refreshed metrics. Wiki globalopt/pipeline/metrics synced.

## [2026-07-05] diagnose+fix | standup_side_05 slip 14.7cm = PHANTOM. Per-effector dig: entirely right_hand, entirely 25 single-frame "plants" (velocity zero-crossings while the hand lifts off in late standup); IK-vs-median ≤0.4cm (plant IS locally stationary), Stage A smooths hand off the 1-frame anchor. NOT the "structural repositioning floor" I claimed earlier — that take was wrong. Fix: plant_min_run=8 stillness debounce in _compute_anchors (still run <8 fr → moving, not plant) + --plant-min-run CLI + PLANT_MIN_RUN=8 pipeline knob (×4 frame-count, 2@30Hz). Standalone standup_side_05 14.7→6.8cm, coll/spikes/track unchanged. Full 18-clip batch re-running with fix → review next session (watch kneelingFall_03 10.7cm). solver+pipeline uncommitted.

## [2026-07-06] export+pipeline | Added 50 Hz IHMC JSON set. `1.json` (IHMC ref) confirmed ~50 Hz, `1.npz` fps=50 (json→npz keeps rate, NOT 100). Regenerated 18 clips from the native-120 grounded NPZs via exporter `--fps 50` (genuine 120→50 downsample, not upsampled-from-30) → `outputs/ihmcJsons50hz/` (all exactly 50.0 Hz). Wired Stage 6b into retargetingPipeline.sh (EXPORT_50HZ=1 default, IHMC_DIR_50 knob). Old `outputs/ihmcJsons/` (30 Hz-origin) left in place but superseded. Wiki ihmc-export.md updated.

## [2026-07-06] diagnose+fix | RDX playback: foot-off-ground + bobbing. Root cause of the floating foot = Stage-3 TARGETS put the two ankles 5.78 cm apart in Z while both feet contact-labelled (inconsistent input a 1-DOF grounding shift can't reconcile), NOT a solver bug. Bobbing = perframe grounding shift wanders 7–9 cm as the lowest contact migrates hands→knees→feet. Fixes (3 stages): (3) `--coplanar-feet-mode mean` snaps both ankle-height targets to a common Z on both-feet-contact frames (achieved ankle gap 4.66→0.95 cm); (4) sole-corner on-floor rows drive each planted foot's 4 corners to a shared floor_z = on-floor+flat+coplanar in one row type (final sole gap →0.54 cm), pin split to X,Y on planted frames, keep-best score gains foot_floor_err + pen gate→2 cm, plant-slip made horizontal-only for feet; (4.5) new grounding mode `constant-contact` (now default) = single shift keyed to planted-foot soles (median) → 0 bobbing AND feet stay down (plain `constant` floats them +9.8 cm grounding on early hands/knees). Result standup_02: worst foot 4.7→0.6 cm off floor, wander 9→0 cm, peak self-pen 0.5→1.4 cm. Only standup_02 regenerated + re-exported; 18-clip re-run pending. Wiki globalopt/contact-first-ik/grounding updated. L/R-mirror-vs-RDX convention unconfirmed.

## [2026-07-06] batch | Full 18-clip coplanar re-run (COPLANAR_FEET_MODE=mean, FLOOR_WEIGHT=200, GROUND_MODE=constant-contact, RENDER=0). ok=18 fail=0, spikes 0. Peak self-pen ≤1.86cm (kneelingFall_02; up from pre-coplanar ≤0.88 — coplanar/floor rows extend get-up legs ~1cm more grazing); shovels+squat 0.00. Feet planted: 7 standup/get-up clips finish both feet within ≈0.6cm of floor, coplanar pair split ≤0.6cm (standup_02 L0.60/R0.06) — RDX floating-foot gone. Caveat: the 13–16cm Stage-B floor_err on natural/side/slideHandsBack is a max-over-planted-frames artefact from early high-plant frames, NOT final stance. Two FALL clips penetrate the floor plane at the end (kneelingFall_02 Lfoot −11cm, kneelingFall_03 Rfoot −15.8cm): late free foot below the single constant-contact shift — separate regime, wants per-frame/hybrid grounding. 18 native-120 + 18 50Hz JSONs regenerated. METHOD.md §9 measured stats updated. Log outputs/logs/pipeline_coplanar.log.

## [2026-07-07] batch+discuss | Two NEW clips, new subject (Luigi): luigi_standProne_03, luigi_standSupine_08 from data/raw/inhouse/LuigiStand/. Full pipeline run via new `CLIPS_MATCH` name-filter env in retargetingPipeline.sh (+ 2 CLIPS entries) so a subset runs without recomputing the 18. Per-subject scaling confirmed OK: both scale calcs are Euclidean-distance ratios from the clip's own first frame (orientation-invariant → new subject + prone/supine rest both fine, no per-subject config). Prabin's read: kinematics good, but NOT usable for RL yet — feet not fully planted / ground penetration up (the documented constant-contact single-shift tradeoff: soft FLOOR_WEIGHT rows pull-not-pin; a ~2cm hovering foot becomes a phantom target the mimic tracker chases). Decisions: (1) real fix = automated foot-lock post-process (proposed Stage 4.6: per-planted-foot IK snap sole→z=0+flat during plant intervals, hold body, blend at boundaries), NOT manual per-clip Blender; (2) teammates trying manual Blender + floated dropping this pipeline — Prabin refuses; framing = our pipeline is the 90% (morphology/IK/orientation/self-collision/smoothing), foot-lock is the last 10%; (3) if Blender stays it should start FROM our grounded NPZ (already robot qpos), not raw human (would re-owe the whole retarget by hand); (4) prone-standup hands-thrown-forward-for-momentum is a motion-specific tracking problem (would need hand-vs-leg reweighting, one motion type only) — but manual Blender is even more motion-specific (one motion, period). Next: prototype Stage 4.6 foot-lock, test on luigi_standProne_03 + a standup clip. retargetingPipeline.sh CLIPS/CLIPS_MATCH change uncommitted. Detail in SESSION_HANDOFF 2026-07-07.

## [2026-07-06] doc | METHOD.md §6: added in-detail the Stage3→Stage4 data hand-off (qpos_ik = smoothing target + contact-anchor source + Stage-B linearisation origin; target_positions/contact_flags reused; achieved-ori/align-errors/self-coll-counts carried but unused), the mesh→QP-row hand-off ("mesh never enters the QP as geometry" — mj_forward reduces mesh to contact points = dist/normal/pos; one row per contact, j_sep coeffs + pen RHS; QP size scales with #contacts not vertices), and the OSQP assembly mapping (Jacobians→P/q/A, pen→l, slack→P penalty+A identity). Wiki globalopt.md gets a summary block pointing to METHOD. Answering a "which QP / why didn't the huge mesh choke it" question. No code change.

## [2026-07-08] diagnose | Foot-slip vs FOOT_WEIGHT resolved — NO tuning. New tool `scripts/diagnose_foot_slip.py` (reuses solver's `_compute_anchors`/`_contact_point`/penetration filter; per-frame foot XY deviation from frozen anchor vs max inter-limb pen, ±6-fr windowed corr per mimic-repo review). Key: the 6.3cm standup_side_04 slip was STALE (pre coplanar+floor); current config = 1.9cm. Diagnostic on fresh re-run: slip↔pen corr −0.08 (collision-FREE, not the leg-crossing prior); FOOT_WEIGHT sweep 160/1000/4000 → slip 1.9/1.7/1.7cm while pen 1.61/2.16/2.46 + coll 8.8/9.7/12.8% climb. Slip floors at 1.7cm = λ_smooth transition-blend at plant EDGES (fr 495→505 ramp), not a weight deficit or collision floor. Decision: FOOT_WEIGHT stays 160, no Stage 4.6, drop the standupTuned footlock branch. Only open q: is 1.7cm below the mimic tracker's reward std. globalopt.md updated.

## [2026-07-08] eval | New corpus artifact table: `scripts/eval_artifacts_corpus.py` (per-clip joint-limit / contact-slip / self-collision, reuses solver anchors+penetration filter at strict COLL_HOPS=2, evaluated on global_opt = grounded-equivalent). CSV `outputs/artifact_table.csv`. Findings across 20 clips: ZERO hard joint violations; NECK_Y systematically pinned 48-63% on standups (head-ori target over-drives neck pitch — the one actionable limit finding); foot slip median-max 2.1cm / p95 1.0cm, worst is FALLS not standups (kneelingFall_03 8.3cm foot; standup_02's 6.1cm is a HAND push-off); self-collision median 8.3% but all shallow grazing (peak pen ≤1.9cm everywhere, kneeling clips drive coll%). Slip split foot(horizontal)/hand(3D). metrics.md updated with the table + methodology.

## [2026-07-08] eval | Added GROUND-contact metrics to eval_artifacts_corpus.py (reads grounded_contactfirst/, floor=z=0): planted-foot penetration vs any-foot(swing) penetration vs planted-foot float. Big reframe — the RL blocker is DOWNWARD ground penetration, not XY slip or float. (1) Planted-foot pen median 2.5cm / max 6.5cm on nearly every clip incl shovels — root cause = constant-contact registers to planted-sole MEDIAN (--contact-percentile 50), so ~half of planted frames sit below floor by construction; fixable by lowering the percentile (trades vs float). (2) Swing/tucked-foot clipping up to 28cm (standup_side_05; non-contact feet during deep crouch, root z=0.21) — single rigid shift can't fix, needs geometry-aware/hybrid grounding (the known fall/get-up gap; matches handoff kneelingFall -11/-15.8cm). (3) Float tiny (median 0.2cm) — grounding errs down not up, so the feared 'hovering plant phantom target' is really a below-floor foot. metrics.md + grounding.md updated.

## [2026-07-08] diagnose/correct | Get-up floor residual is BETWEEN-PHASE, not within-plant Z-drift (corrected an earlier read). Per-plant-window split: feet frozen 0.2-0.6cm WITHIN each window; multi-cm spread is entirely BETWEEN windows at different postural phases (lying/kneeling root-z~0 vs standing root-z~0.5). Implemented+tested a Z-anchor freeze (restore full-3D body pin in _build_contact so Z gets XY's frozen-median anchor) on Luigi clips -> moved standSupine_08 spread 3.51->2.89cm only (real geometry, not soft-anchor gap) -> REVERTED. Real fix = grounding registration to the TERMINAL standing stance: for get-ups that's a HIGHER contact-percentile (opposite of the earlier p25 direction, which floats the functional stance). luigi_standSupine_08 p50 standing floats +2.56cm -> p70 plants +0.12cm (lying penetrates 3.4cm, fine/non-weight-bearing); p70 keeps luigi_standProne_03 planted (-0.15cm). standProne_03 ground already fine, only residual is 4.2cm XY slip (separate). Principled unbuilt fix: register on highest-root-z plant window. grounding.md updated. notes.md replaced.

## [2026-07-08] diagnose | Foot-frame sole-normal bug (settled). Canonical foot frame sets x=toe-ankle (bone axis, declined ~18deg below horizontal since ankle sits above the grounded toe), z=x*y, so "sole normal" reads ~18deg from vertical for a FLAT foot + ~4deg L/R skew — frame geometry, not motion (constant across 20 clips + both subjects; arctan(4.6/14)=18.2deg matches measured 18.7deg). Impact confined to Stage-3 contact-detection tilt gate (world-delta transfer + robot-side foot-flat enforcement unaffected). Fix = per-foot SELF-CALIBRATED baseline subtraction (gate on tilt - p15 of that foot's planted-frame tilt), NOT projecting x onto horizontal (proven to zero the pitch signal: flat/+10/-10 all read 0deg projected, since toe-ankle vertical component = baseline+pitch summed). No heel marker (ankle/toe/toe_end only) so no two-ground-point sole plane; baseline subtraction is the only pitch-recovering option. Post-calibration corpus re-audit: 258/286 plant windows <3deg above baseline (clean flat), phantoms 6-16deg (luigi_standSupine supine foot 7.6deg = genuinely non-flat). 3-6deg gap = 14 windows -> default LEAVE (don't force). Design converged w/ mimic-Claude: build (1) frame self-calibration first, then (2) confidence-gated per-segment ground enforcement (force z=0 only when tilt-above-baseline<3deg; >=3deg leave). orientation-frames.md updated. Nothing in pipeline changed yet.

## [2026-07-08] BUILD (1) | Baseline-relative flat gate SHIPPED in detect_contacts_from_human (solve_fbx_canonical_alex_contactfirst.py). Was raw `tilt<40deg` (nearly inert given the ~18deg toe-ankle frame offset); now per-foot self-calibrated baseline (p15 of candidate-contact tilt) + gate `tilt-baseline < --foot-flat-margin` (default 6deg), absolute --foot-flat-tilt=40 as <20-candidate fallback. Corpus detection-only check (20 clips, pre-IK so seconds): (a) 4 real-plant frames dropped (1 borderline window), (b) 3 phantom frames leaked, (c) 0/40 feet hit fallback -> self-calib universally applicable. Validated on Luigi: standSupine_08 supine phantom 100%->0% dropped, standing preserved, Stage-4 floor_err 14.3->0.68cm; standProne_03 no regression. NECESSARY-not-sufficient: grounding float persists (+2.56->+2.03cm) because genuinely-FLAT contacts remain at different heights across phases (rise -11.5 vs stand -7.2cm) — that's build (2)'s job (confidence-gated per-segment ground enforcement, force z=0 only when tilt-above-baseline<3deg). contact-first-ik.md + orientation-frames.md updated. Existing outputs/ NPZs still hold OLD labels; take effect on next Stage-3 run.

## [2026-07-08] BUILD (2) | Redirected by measurement: it's a GROUNDING-registration fix, NOT a QP change. Prereq (debounce vs new segment lengths): clear — post-relabel genuine flat plants stay long/contiguous (100-600fr), only 1-4fr spurious blips fall below contact_min_run=12/plant_min_run=8. Key measurement: calibrated Stage-4 solve ALREADY plants clean stances consistently at floor_z (standSupine_08 both stances -6.90/-6.92 = floor_z -6.91, floor_err 0.68cm); the rise-phase low contact is correctly reclassified MOVING by _compute_anchors, QP never enforces it. So no per-segment QP enforcement needed (contradicts mimic-Claude's "keep in QP" premise, which assumed the solve left segments apart — the label fix disproved it). Residual +2cm float was a grounding-registration MISMATCH: constant-contact keyed on RAW contact_flags (incl. moving rise-phase low frames) not the still stances. FIX (shipped in post_process_ground_contactfirst.py _planted_foot_sole_samples): still-filter — foot only anchors registration on frames where its body speed < --still-speed (0.05, = solver plant-speed), per-foot fallback to labelled if no still frames. standSupine_08 standing +2.02->+0.04cm; regression spot-check (shovel/side/fall/standup) <=0.2cm = noop where not needed. Two-phase plan complete: (1) baseline-relative flat labels + (2) still-filtered registration. PENDING: full corpus Stage-3+4+ground re-run (labels changed) + regenerate stale artifact table. grounding.md updated.
