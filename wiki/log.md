# Wiki Log

Append-only. Format: `## [YYYY-MM-DD] <op> | <what>` — parseable via `grep "^## \[" log.md | tail -5`.

## [2026-07-03] init | Wiki created
Seeded from METHOD.md, SESSION_HANDOFF.md, README.md, paper_idea.md, paperIdea2.md, repo scan.
18 pages: concepts (9), data (2), experiments (3), results (2), questions (2). CLAUDE.md slimmed to
session protocol + critical conventions; details moved here.

## [2026-07-03] ingest | Undermind lit report → paperIdea3.md (Humanoids 2027)
Read the 38-page Undermind deep-research report (170 refs): niche confirmed unoccupied, 4 concrete
gaps (floor-stance templates, contact sequencing, knee/pelvis target editing, kinematic hardware
playback). Consolidated paper_idea.md + paperIdea2.md into paperIdea3.md — "Any-Contact Retargeting"
full paper for Humanoids 2027 (C1 generalized contacts, C2 template library, C3 quasi-static CoM,
C4 hardware playback; 12-month plan, BeyondMimic gate Oct 2026). New wiki page concepts/related-work;
questions/publication rewritten as two tracks.

## [2026-07-03] lint | Old idea docs deleted by Prabin
`LLM Wiki.md`, `paper_idea.md` (untracked, gone), `paperIdea2.md` (tracked — recover via
`git show HEAD:paperIdea2.md`). Content absorbed into paperIdea3.md + wiki. Dead refs fixed in
CLAUDE.md, README.md, questions/publication.md.

## [2026-07-03] write | paper_intro.md drafted + SESSION_HANDOFF rewritten lean
Introduction v0.1 for the 2027 Any-Contact paper (intro→background→lit→gap→contributions, [TBD-*]
placeholders, revision notes). Handoff slimmed to session-delta + uncommitted state + NEXT; wiki is
now the technical record. Next session: fullURDF render inspection, C1 (generalized contacts),
mentor items.

## [2026-07-03] fix | IHMC export fps bug: src_fps now real-time (fps/stride=30), not capture 120. Regenerated all 18 outputs/ihmcJsons-120hz/ at correct 4x duration.

## [2026-07-05] analyze | rsl_rl consumer traced: IHMC RL tracker consumes at 50 Hz (json_to_npz --output_fps 50, ZOH no-interp; replay dt=0.02; all ref motions fps=50). 30 Hz solve is sub-Nyquist → self-upsample 30→120 launders lost content. Plan: solve at ≥50 Hz (STRIDE=1→120 or 2→60), downsample only at render/consumer. Watch LAMBDA_SMOOTH dt-retune. Wiki ihmc-export + pipeline updated.

## [2026-07-05] change | Native 120 Hz solve: STRIDE 4→1, LAMBDA_SMOOTH 20→320 (×16 ∝fps²), GROUND_SMOOTH 5→80, N_OUTER 3→6, contact min-run/ramp/preroll ×4. Fixed OSQP status bug ("solved inaccurate" space vs underscore) + max_iter 8000→20000. Validated on 3 clips vs archived 30 Hz baseline (scaling correct; standup ~9cm slip pre-existing; get-up coll needs n_outer=6). Full 18-clip batch run; 30 Hz baseline archived outputs/*_30hz/. Review next session.

## [2026-07-05] diagnose+fix | SCA parity bug → keep-best-iterate (slip-aware). Reviewing the 120 Hz batch found the get-up self-collision "regression" was NOT frame rate and NOT OSQP: the SCA loop re-linearizes collision only at each outer's start, so a collision-free outer drops all collision rows and takes an unconstrained step back into ~6 cm penetration → per-outer penetration OSCILLATES, and returning the last iterate makes the result depend on n_outer PARITY (30 Hz n=3 odd=lucky, 120 Hz n=6 even=bad). Verbose re-solve of standup_side_04 proved it (outers 3&5 hit 0.00 cm, discarded; outer 6 shipped 6.59 cm). Fix: stage_b returns the best iterate across outers, scored lexicographically (pen-beyond-1cm, then pen+slip, then coll%), seeded with the Stage-A warm start.

## [2026-07-05] tune | Plant-slip reduction. plant-speed sweep (0.05/0.04/0.03) = DEAD lever (slip 10.4→10.9 cm, slow steady drift stays sub-threshold). Pin-weight sweep IS the lever: foot/hand ×4 (40/8→160/32) cut standup_side_04 slip 10.4→6.3 cm for <1 cm shallow collision; ×16 barely beats ×4 (diminishing). Finalized #3 = pins ×4 + slip-aware keep-best. Pipeline: added FOOT_WEIGHT/HAND_WEIGHT knobs + Stage 6 IHMC export (outputs/ihmcJsons-native120hz, native 120 no --fps).

## [2026-07-05] batch | Finalized 18-clip run, ok=18 fail=0 (clip 11 shovel_rightbucket_01 no longer crashes). Peak self-penetration ≤0.88 cm + spikes 0 on ALL 18. Slip outliers: standup_side_05 14.7 cm/38% grazing, kneelingFall_03 10.7 cm — contact-labeling floor, not regression. 18 IHMC JSONs regenerated native 120. Log outputs/logs/pipeline_native120_20260705_141837.log.

## [2026-07-05] doc | METHOD.md updated to shipped reality: header (branch main, single fullmesh model, no _v2/_v2_fullmesh/--soft-collision), Stage-6 export subsection §7.1, §6.2 keep-best+slip-aware selection + pins ×4, new §6.4 native-120 rate-scaling table+derivation, §8 model consolidation, §9 refreshed metrics. Wiki globalopt/pipeline/metrics synced.

## [2026-07-05] diagnose+fix | standup_side_05 slip 14.7cm = PHANTOM. Per-effector dig: entirely right_hand, entirely 25 single-frame "plants" (velocity zero-crossings while the hand lifts off in late standup); IK-vs-median ≤0.4cm (plant IS locally stationary), Stage A smooths hand off the 1-frame anchor. NOT the "structural repositioning floor" I claimed earlier — that take was wrong. Fix: plant_min_run=8 stillness debounce in _compute_anchors (still run <8 fr → moving, not plant) + --plant-min-run CLI + PLANT_MIN_RUN=8 pipeline knob (×4 frame-count, 2@30Hz). Standalone standup_side_05 14.7→6.8cm, coll/spikes/track unchanged. Full 18-clip batch re-running with fix → review next session (watch kneelingFall_03 10.7cm). solver+pipeline uncommitted.

## [2026-07-06] export+pipeline | Added 50 Hz IHMC JSON set. `1.json` (IHMC ref) confirmed ~50 Hz, `1.npz` fps=50 (json→npz keeps rate, NOT 100). Regenerated 18 clips from the native-120 grounded NPZs via exporter `--fps 50` (genuine 120→50 downsample, not upsampled-from-30) → `outputs/ihmcJsons50hz/` (all exactly 50.0 Hz). Wired Stage 6b into retargetingPipeline.sh (EXPORT_50HZ=1 default, IHMC_DIR_50 knob). Old `outputs/ihmcJsons/` (30 Hz-origin) left in place but superseded. Wiki ihmc-export.md updated.

## [2026-07-06] diagnose+fix | RDX playback: foot-off-ground + bobbing. Root cause of the floating foot = Stage-3 TARGETS put the two ankles 5.78 cm apart in Z while both feet contact-labelled (inconsistent input a 1-DOF grounding shift can't reconcile), NOT a solver bug. Bobbing = perframe grounding shift wanders 7–9 cm as the lowest contact migrates hands→knees→feet. Fixes (3 stages): (3) `--coplanar-feet-mode mean` snaps both ankle-height targets to a common Z on both-feet-contact frames (achieved ankle gap 4.66→0.95 cm); (4) sole-corner on-floor rows drive each planted foot's 4 corners to a shared floor_z = on-floor+flat+coplanar in one row type (final sole gap →0.54 cm), pin split to X,Y on planted frames, keep-best score gains foot_floor_err + pen gate→2 cm, plant-slip made horizontal-only for feet; (4.5) new grounding mode `constant-contact` (now default) = single shift keyed to planted-foot soles (median) → 0 bobbing AND feet stay down (plain `constant` floats them +9.8 cm grounding on early hands/knees). Result standup_02: worst foot 4.7→0.6 cm off floor, wander 9→0 cm, peak self-pen 0.5→1.4 cm. Only standup_02 regenerated + re-exported; 18-clip re-run pending. Wiki globalopt/contact-first-ik/grounding updated. L/R-mirror-vs-RDX convention unconfirmed.

## [2026-07-06] batch | Full 18-clip coplanar re-run (COPLANAR_FEET_MODE=mean, FLOOR_WEIGHT=200, GROUND_MODE=constant-contact, RENDER=0). ok=18 fail=0, spikes 0. Peak self-pen ≤1.86cm (kneelingFall_02; up from pre-coplanar ≤0.88 — coplanar/floor rows extend get-up legs ~1cm more grazing); shovels+squat 0.00. Feet planted: 7 standup/get-up clips finish both feet within ≈0.6cm of floor, coplanar pair split ≤0.6cm (standup_02 L0.60/R0.06) — RDX floating-foot gone. Caveat: the 13–16cm Stage-B floor_err on natural/side/slideHandsBack is a max-over-planted-frames artefact from early high-plant frames, NOT final stance. Two FALL clips penetrate the floor plane at the end (kneelingFall_02 Lfoot −11cm, kneelingFall_03 Rfoot −15.8cm): late free foot below the single constant-contact shift — separate regime, wants per-frame/hybrid grounding. 18 native-120 + 18 50Hz JSONs regenerated. METHOD.md §9 measured stats updated. Log outputs/logs/pipeline_coplanar.log.

## [2026-07-07] batch+discuss | Two NEW clips, new subject (Luigi): luigi_standProne_03, luigi_standSupine_08 from data/raw/inhouse/LuigiStand/. Full pipeline run via new `CLIPS_MATCH` name-filter env in retargetingPipeline.sh (+ 2 CLIPS entries) so a subset runs without recomputing the 18. Per-subject scaling confirmed OK: both scale calcs are Euclidean-distance ratios from the clip's own first frame (orientation-invariant → new subject + prone/supine rest both fine, no per-subject config). Prabin's read: kinematics good, but NOT usable for RL yet — feet not fully planted / ground penetration up (the documented constant-contact single-shift tradeoff: soft FLOOR_WEIGHT rows pull-not-pin; a ~2cm hovering foot becomes a phantom target the mimic tracker chases). Decisions: (1) real fix = automated foot-lock post-process (proposed Stage 4.6: per-planted-foot IK snap sole→z=0+flat during plant intervals, hold body, blend at boundaries), NOT manual per-clip Blender; (2) teammates trying manual Blender + floated dropping this pipeline — Prabin refuses; framing = our pipeline is the 90% (morphology/IK/orientation/self-collision/smoothing), foot-lock is the last 10%; (3) if Blender stays it should start FROM our grounded NPZ (already robot qpos), not raw human (would re-owe the whole retarget by hand); (4) prone-standup hands-thrown-forward-for-momentum is a motion-specific tracking problem (would need hand-vs-leg reweighting, one motion type only) — but manual Blender is even more motion-specific (one motion, period). Next: prototype Stage 4.6 foot-lock, test on luigi_standProne_03 + a standup clip. retargetingPipeline.sh CLIPS/CLIPS_MATCH change uncommitted. Detail in SESSION_HANDOFF 2026-07-07.

## [2026-07-06] doc | METHOD.md §6: added in-detail the Stage3→Stage4 data hand-off (qpos_ik = smoothing target + contact-anchor source + Stage-B linearisation origin; target_positions/contact_flags reused; achieved-ori/align-errors/self-coll-counts carried but unused), the mesh→QP-row hand-off ("mesh never enters the QP as geometry" — mj_forward reduces mesh to contact points = dist/normal/pos; one row per contact, j_sep coeffs + pen RHS; QP size scales with #contacts not vertices), and the OSQP assembly mapping (Jacobians→P/q/A, pen→l, slack→P penalty+A identity). Wiki globalopt.md gets a summary block pointing to METHOD. Answering a "which QP / why didn't the huge mesh choke it" question. No code change.

## [2026-07-08] diagnose | Foot-slip vs FOOT_WEIGHT resolved — NO tuning. New tool `scripts/diagnose_foot_slip.py` (reuses solver's `_compute_anchors`/`_contact_point`/penetration filter; per-frame foot XY deviation from frozen anchor vs max inter-limb pen, ±6-fr windowed corr per mimic-repo review). Key: the 6.3cm standup_side_04 slip was STALE (pre coplanar+floor); current config = 1.9cm. Diagnostic on fresh re-run: slip↔pen corr −0.08 (collision-FREE, not the leg-crossing prior); FOOT_WEIGHT sweep 160/1000/4000 → slip 1.9/1.7/1.7cm while pen 1.61/2.16/2.46 + coll 8.8/9.7/12.8% climb. Slip floors at 1.7cm = λ_smooth transition-blend at plant EDGES (fr 495→505 ramp), not a weight deficit or collision floor. Decision: FOOT_WEIGHT stays 160, no Stage 4.6, drop the standupTuned footlock branch. Only open q: is 1.7cm below the mimic tracker's reward std. globalopt.md updated.

## [2026-07-08] eval | New corpus artifact table: `scripts/eval_artifacts_corpus.py` (per-clip joint-limit / contact-slip / self-collision, reuses solver anchors+penetration filter at strict COLL_HOPS=2, evaluated on global_opt = grounded-equivalent). CSV `outputs/artifact_table.csv`. Findings across 20 clips: ZERO hard joint violations; NECK_Y systematically pinned 48-63% on standups (head-ori target over-drives neck pitch — the one actionable limit finding); foot slip median-max 2.1cm / p95 1.0cm, worst is FALLS not standups (kneelingFall_03 8.3cm foot; standup_02's 6.1cm is a HAND push-off); self-collision median 8.3% but all shallow grazing (peak pen ≤1.9cm everywhere, kneeling clips drive coll%). Slip split foot(horizontal)/hand(3D). metrics.md updated with the table + methodology.

## [2026-07-08] eval | Added GROUND-contact metrics to eval_artifacts_corpus.py (reads grounded_contactfirst/, floor=z=0): planted-foot penetration vs any-foot(swing) penetration vs planted-foot float. Big reframe — the RL blocker is DOWNWARD ground penetration, not XY slip or float. (1) Planted-foot pen median 2.5cm / max 6.5cm on nearly every clip incl shovels — root cause = constant-contact registers to planted-sole MEDIAN (--contact-percentile 50), so ~half of planted frames sit below floor by construction; fixable by lowering the percentile (trades vs float). (2) Swing/tucked-foot clipping up to 28cm (standup_side_05; non-contact feet during deep crouch, root z=0.21) — single rigid shift can't fix, needs geometry-aware/hybrid grounding (the known fall/get-up gap; matches handoff kneelingFall -11/-15.8cm). (3) Float tiny (median 0.2cm) — grounding errs down not up, so the feared 'hovering plant phantom target' is really a below-floor foot. metrics.md + grounding.md updated.

## [2026-07-08] diagnose/correct | Get-up floor residual is BETWEEN-PHASE, not within-plant Z-drift (corrected an earlier read). Per-plant-window split: feet frozen 0.2-0.6cm WITHIN each window; multi-cm spread is entirely BETWEEN windows at different postural phases (lying/kneeling root-z~0 vs standing root-z~0.5). Implemented+tested a Z-anchor freeze (restore full-3D body pin in _build_contact so Z gets XY's frozen-median anchor) on Luigi clips -> moved standSupine_08 spread 3.51->2.89cm only (real geometry, not soft-anchor gap) -> REVERTED. Real fix = grounding registration to the TERMINAL standing stance: for get-ups that's a HIGHER contact-percentile (opposite of the earlier p25 direction, which floats the functional stance). luigi_standSupine_08 p50 standing floats +2.56cm -> p70 plants +0.12cm (lying penetrates 3.4cm, fine/non-weight-bearing); p70 keeps luigi_standProne_03 planted (-0.15cm). standProne_03 ground already fine, only residual is 4.2cm XY slip (separate). Principled unbuilt fix: register on highest-root-z plant window. grounding.md updated. notes.md replaced.

## [2026-07-08] diagnose | Foot-frame sole-normal bug (settled). Canonical foot frame sets x=toe-ankle (bone axis, declined ~18deg below horizontal since ankle sits above the grounded toe), z=x*y, so "sole normal" reads ~18deg from vertical for a FLAT foot + ~4deg L/R skew — frame geometry, not motion (constant across 20 clips + both subjects; arctan(4.6/14)=18.2deg matches measured 18.7deg). Impact confined to Stage-3 contact-detection tilt gate (world-delta transfer + robot-side foot-flat enforcement unaffected). Fix = per-foot SELF-CALIBRATED baseline subtraction (gate on tilt - p15 of that foot's planted-frame tilt), NOT projecting x onto horizontal (proven to zero the pitch signal: flat/+10/-10 all read 0deg projected, since toe-ankle vertical component = baseline+pitch summed). No heel marker (ankle/toe/toe_end only) so no two-ground-point sole plane; baseline subtraction is the only pitch-recovering option. Post-calibration corpus re-audit: 258/286 plant windows <3deg above baseline (clean flat), phantoms 6-16deg (luigi_standSupine supine foot 7.6deg = genuinely non-flat). 3-6deg gap = 14 windows -> default LEAVE (don't force). Design converged w/ mimic-Claude: build (1) frame self-calibration first, then (2) confidence-gated per-segment ground enforcement (force z=0 only when tilt-above-baseline<3deg; >=3deg leave). orientation-frames.md updated. Nothing in pipeline changed yet.

## [2026-07-08] BUILD (1) | Baseline-relative flat gate SHIPPED in detect_contacts_from_human (solve_fbx_canonical_alex_contactfirst.py). Was raw `tilt<40deg` (nearly inert given the ~18deg toe-ankle frame offset); now per-foot self-calibrated baseline (p15 of candidate-contact tilt) + gate `tilt-baseline < --foot-flat-margin` (default 6deg), absolute --foot-flat-tilt=40 as <20-candidate fallback. Corpus detection-only check (20 clips, pre-IK so seconds): (a) 4 real-plant frames dropped (1 borderline window), (b) 3 phantom frames leaked, (c) 0/40 feet hit fallback -> self-calib universally applicable. Validated on Luigi: standSupine_08 supine phantom 100%->0% dropped, standing preserved, Stage-4 floor_err 14.3->0.68cm; standProne_03 no regression. NECESSARY-not-sufficient: grounding float persists (+2.56->+2.03cm) because genuinely-FLAT contacts remain at different heights across phases (rise -11.5 vs stand -7.2cm) — that's build (2)'s job (confidence-gated per-segment ground enforcement, force z=0 only when tilt-above-baseline<3deg). contact-first-ik.md + orientation-frames.md updated. Existing outputs/ NPZs still hold OLD labels; take effect on next Stage-3 run.

## [2026-07-08] BUILD (2) | Redirected by measurement: it's a GROUNDING-registration fix, NOT a QP change. Prereq (debounce vs new segment lengths): clear — post-relabel genuine flat plants stay long/contiguous (100-600fr), only 1-4fr spurious blips fall below contact_min_run=12/plant_min_run=8. Key measurement: calibrated Stage-4 solve ALREADY plants clean stances consistently at floor_z (standSupine_08 both stances -6.90/-6.92 = floor_z -6.91, floor_err 0.68cm); the rise-phase low contact is correctly reclassified MOVING by _compute_anchors, QP never enforces it. So no per-segment QP enforcement needed (contradicts mimic-Claude's "keep in QP" premise, which assumed the solve left segments apart — the label fix disproved it). Residual +2cm float was a grounding-registration MISMATCH: constant-contact keyed on RAW contact_flags (incl. moving rise-phase low frames) not the still stances. FIX (shipped in post_process_ground_contactfirst.py _planted_foot_sole_samples): still-filter — foot only anchors registration on frames where its body speed < --still-speed (0.05, = solver plant-speed), per-foot fallback to labelled if no still frames. standSupine_08 standing +2.02->+0.04cm; regression spot-check (shovel/side/fall/standup) <=0.2cm = noop where not needed. Two-phase plan complete: (1) baseline-relative flat labels + (2) still-filtered registration. PENDING: full corpus Stage-3+4+ground re-run (labels changed) + regenerate stale artifact table. grounding.md updated.

## [2026-07-09] diagnose | Step-0 target-space diagnostic corrects the ground-penetration diagnosis: foot = free-swing tracking sag + toe-down dig (targets fine, floor term needed); hand = palm-pin target below floor (clamp needed); prior Stage-3 failure = floor placed 8cm too high. Plan: collisionFixPlan.md; tool: scripts/diagnose_floor_penetration.py

## [2026-07-10] fix | Ground-penetration + wrist-flick RESOLVED for luigi_standProne_03: target-space floor placement (Stage 3) + palm-pin clamp + floor repulsion (no ramp -> wrist limit-slam, fixed via targeted 2-pass arm refinement around floor-onset windows: _detect_arm_floor_onset_windows/refine_arm_floor_transitions) + Stage-4 mesh floor collision. Result 11.5cm->2.4cm pen, 0 spikes (was 0->1 spike mid-fix, now 0). luigi_standSupine_08 explicitly excluded (already clean baseline; hard floor-collision made it worse due to lying/standing multi-phase floor-height conflict, see grounding.md). Per-clip only, not corpus-wide. Deliverables regenerated via real pipeline.

## [2026-07-10] fix | Phase-aware floor collision RESOLVES luigi_standSupine_08 too (follow-up to same-day entry above): floor_phase_weight() (both solver scripts) — smoothstep of pelvis/root height between clip-low and planted-foot/standing reference, gates hard floor-collision rows+counting per frame instead of clip-wide. Fixes the prior session's exclusion reason (false 14.4cm RIGHT_HIP_X_LINK "penetration" during lying phase) AND catches a real, previously-invisible bug: baseline RIGHT_FOOT genuinely clipped 4.4cm through the floor during the stand-up transition (frames 758-772), invisible to eval_artifacts_corpus.py's planted-foot-only check. Cost: small boundary artifact (~5 frames, self-pen to 2.1cm, no spikes) + real slip/flat-error increase (1.6->4.3cm, 0->5.2deg) — accepted trade. Still 0 spikes. No-op for single-phase clips (luigi_standProne_03 outputs confirmed byte-unchanged). Shipped via real pipeline. NOT done: Stage 4.5's grounding percentile is separately phase-blind (grounding.md's p70-for-get-ups recommendation, unbuilt) — used pipeline default p50 for this deliverable, orthogonal follow-up if needed.

## [2026-07-10] design | Committed phase-aware floor collision anyway despite mixed real-world result ("Tried phase aware floor collision, didn't work, keeping code", commit 0d79f53) — the per-clip opt-in flag pattern (`--floor-weight/--floor-refine/--floor-phase-aware` on 2 of 20 CLIPS[] entries) was judged unsustainable regardless. New branch `phasic-v2` started for a ground-up redesign: floor height established upstream as an invariant (canonical-human grounding, support point = lowest marker not joint, targets pinned to z=0) instead of estimated per-stage; pipeline decoupled into strictly-contracted phases (P0 canonical prep -> P1 contact IK -> P2 smoothing -> P3 physics-plausibility slot -> P4 per-limb cleanup owning floor/self-collision/swing-clearance -> P5 validate/export), each independently ablatable. Full plan: `plan.md` (repo root); execution log: `planLog.md`. M0 (branch + frozen 20-clip baseline, `wiki/experiments/phasic-v2-baseline.md`) done, ok=20/fail=0. Orientation stays semantic-frames (settled, not revisited — FBX rotations rejected as bind-pose/vendor-specific, axial DOFs already recovered via hand thumb landmark + elbow/knee position pins). Physics phase scoped as a flag-gated plausibility QP (vel/accel/CoM-in-polygon), not a dynamics sim.

## [2026-07-10] feat | phasic-v2 M1 done: Stage 2.5 canonical grounding + persisted contact labels (P0 phase). New `scripts/contact_labels.py` (CONTACT_EFFECTORS, detect_contacts_from_human, debounce_flags, ramp_envelope extracted from Stage 3, now shared — T1.1, verified byte-identical output on 3 clips after a stale-cache confound was found and corrected, see planLog.md). New `scripts/ground_canonical_human.py` (Stage 2.5): floor = median of still-plant support-point heights (lowest marker, feet+hands, speed<0.05 debounced >=8 frames — mirrors Stage-4's `_compute_anchors`/Stage-4.5's `_planted_foot_sole_samples`), one rigid shift -> floor=0 by construction in canonical-human space, before morphology scaling. Wired into `retargetingPipeline.sh` as an always-recomputed step (new 5th `ground_extra` CLIPS[] field); Stage 3 now consumes persisted `contact_flags` instead of re-detecting, floor_z hardcoded 0.0 (old 1st-percentile estimate deleted), with a backward-compat fallback + WARNING if given ungrounded input. luigi_standProne_03's onset-hysteresis override moved from Stage-3 solve_extra (now dead there) to the new Stage-2.5 ground_extra field, closing a 1-2/802-frame detection gap to 0/802. Full 20-clip fresh regeneration: ok=20/fail=0, 0 hard joint violations (`wiki/experiments/phasic-v2-M1-gate.md`). One anomaly flagged (not fixed, not in scope): standup_slideHandsBack_03's plPen% jumped to 100% (small systematic offset, plausibly morphology scaling not yet floor-preserving) — expected pre-M2/M3, explicitly flagged to check at the M2 gate. Full plan: `plan.md`; build/verify trail: `planLog.md`.

## [2026-07-10] feat+partial | phasic-v2 M2 partial: T2.1 (per-window target-space floor correction) DONE and verified safe; T2.2 (zero Luigi per-clip flags) FAILS, reverted, reported (not silently worked around). T2.1 generalizes collisionFixPlan.md's Fix A/B: feet now get a NEW per-window floor clamp (never had one before, only coplanar-snap); hands keep the ORIGINAL clip-wide foot-derived clamp exactly (tested hand-own-data per-window AND clip-wide, both regressed standup_01 plPen% 0%->35.6%/coll% 9.0%->20.4% -- hands have no ankle_clearance-equivalent fixed constant and no foot-hold-style freeze, so a self-referential estimate is less reliable than the original foot-derived one). Two real bugs found+fixed during verification: (1) unit mismatch -- per-window foot reference wrongly had ankle_clearance subtracted then compared against ankle-space targets, ~8cm too permissive; (2) the hand-own-data regression above. Added the plan's explicit gate canary ("assert no contact target ever below floor") -- PASS on all 20 clips. T2.2: directly tested zeroing luigi_standProne_03's Stage-3 solve_extra -- Stage-4 output regresses to pen=14.29cm + 3 surviving spikes (vs historical 2.4cm/0 spikes); confirmed via A/B against the true unmodified script that this is NOT a T2.1 bug (same regression on unmodified code with --floor-weight 0). Matches a risk plan.md's own "Risks/fallbacks" section already flagged, explicitly deferred to M6 ("re-enable mild Stage-3 repulsion default-on WITH ramp -- decision at M6, data-driven"). Reverted the test, left both Luigi CLIPS[] entries' per-clip flags intact -- NOT a silent gap, plan.md's own gate-failure process says stop and report rather than tune around it. Full 20-clip regeneration (Luigi flags still active): ok=20/fail=0, 0 hard joint violations, mixed small deltas vs M1 baseline, no new catastrophic failures (`wiki/experiments/phasic-v2-M2-gate.md`). standup_slideHandsBack_03's M1-flagged 100% plPen% anomaly UNCHANGED -- still unresolved, carry to M3. Full build/debug trail: `planLog.md`.

## [2026-07-10] fix | phasic-v2 T2.2 resolved (follow-up to same-day M2-partial entry, per user instruction to address rather than defer to M6): S3_FLOOR_WEIGHT default changed 0->10 in retargetingPipeline.sh -- mild Stage-3 floor repulsion now applies to ALL 20 clips globally, not gated behind per-clip flags. --floor-refine's ramp mechanism (proven-safe two-pass local arm re-solve) was already default-ON, just never exercised at weight 0. Weight-swept 5->10 on luigi_standProne_03 (5 left 3 residual spikes, 10 reaches 0). luigi_standSupine_08 needed --floor-phase-aware restored (dropping it regressed plPen 0.8->5.6cm -- the multi-phase mechanism genuinely needed for this clip). Second bug found during corpus verification: shipped luigi_standProne_03 config still had 4 spikes despite a clean manual test -- root cause was --contact-preroll 0 (unrelated to floor, a Stage-3 anticipation param) silently dropped when solve_extra was emptied, since the pipeline default is 8. Restored it. Final: luigi_standProne_03 solve_extra = --contact-preroll 0 only (down from 5 flags, zero floor-specific); luigi_standSupine_08 = --floor-phase-aware only (down from 4). Full 20-clip regen: ok=20/fail=0, 0 hard joint violations, 0 velocity spikes corpus-wide (independent direct qpos check, not just eval_artifacts_corpus.py) -- first run exercising the new global floor-repulsion default on all 20 clips, not just Luigi. Mixed small tradeoffs (floor penetration vs self-collision shifts) on several non-Luigi clips, none crossing into hard violations or spikes. standup_slideHandsBack_03's plPen%=100% anomaly (flagged since M1) UNCHANGED across three different mechanism changes now -- likely not a Stage-3 floor problem, carried to M3 as a specific open question (plausibly Stage-4's floor-mode estimate). Full results: `wiki/experiments/phasic-v2-M2-gate.md` (updated in place). M2 now fully done (T2.1 + T2.2).

## [2026-07-10] design | phasic-v2 M3 done via decision, not code change. T3.2 (--floor-collision/--floor-phase-aware default OFF) was ALREADY satisfied pre-M3 (retargetingPipeline.sh's FLOOR_COLLISION env already defaulted off; only Luigi clips opt in). T3.1 ("--floor-mode estimate -> fixed floor_z=0") tested directly and found EMPIRICALLY BROKEN: Alex's own achieved-qpos root frame is a separate coordinate space from the canonical-human floor invariant (M1's Stage 2.5) -- floor_z under estimate mode ranges -0.05 (standing) to -0.88 (shovel/kneeling) corpus-wide, and a single clip's own root Z spans e.g. 0.008->0.816 within itself (lying->standing). This was already implicitly documented at M0 ("shovel/kneeling clips sit far below 0... expected, not a bug") but not connected to T3.1's literal wording until tested: forcing floor_z=0 (--floor-mode zero) on standup_01 (best-case, smallest offset) produced 17.65cm penetration/100% self-collision/1 spike. Reported finding + 3 options to the user; user chose to keep --floor-mode estimate as-is and mark T3.1 done via documented decision -- the estimate is already a legitimate, self-consistent, freshly-per-run-derived floor reference from Stage 3's OWN now-floor-corrected output (M2), not an independent/stale/phase-blind one the way Stage 3's pre-M2 floor_z was. No code changes; no new corpus run needed (M2/T2.2's final state stands). New FOOTGUN documented in wiki/concepts/globalopt.md for future sessions. Full reasoning: planLog.md M3 section.

## [2026-07-10] feat | phasic-v2 M4/T4.1 done: physics plausibility pass (P3 phase, Increment 1). New `scripts/physics_plausibility_pass.py`, opt-in Stage 4.6 in retargetingPipeline.sh (PHYSICS_PASS=on/off, default off, confirmed byte-identical no-op when off via md5sum). Decision variable delta_Q in R^{T*35} (MuJoCo tangent-space convention, 6 root + 29 actuated -- wider than Stage 4's own 29-actuated-only QP since root vel/accel is required); mj_differentiatePos/mj_integratePos handle the free-joint quaternion correctly, no hand-rolled rotation math. Least-perturbation QP (minimize ||delta_Q||^2) subject to hard vel/accel box rows, same banded/tridiagonal 3-point-stencil pattern as Stage 4's _build_smoothness_hessian. Limits NOT model-derived (no <actuator> section exists in the XML) -- calibrated from observed peaks on 4 clips (~2-3x headroom): JOINT_VEL_LIMIT=25 JOINT_ACC_LIMIT=400 ROOT_LIN_VEL_LIMIT=3.0 ROOT_LIN_ACC_LIMIT=10.0 ROOT_ANG_VEL_LIMIT=6.0 ROOT_ANG_ACC_LIMIT=20.0 rad/s or rad/s^2 or m/s or m/s^2 as appropriate. Bug found+fixed during verification: root-angular linearization residual (quaternion retraction is exact for Euclidean DOFs, ~1% nonlinearity residual for orientation specifically) caused a false post-hoc gate failure on luigi_standProne_03 -- fixed via standard bound-tightening (10% internal-only margin on root-angular channels, reported/verified limits stay at true nominal values). Verified: near-no-op on already-clean input, genuinely engages+corrects when limits are deliberately tightened, full 20-clip PASS both standalone and through the real pipeline (17/20 clips 0 rows ever engage, 3 clips show small real engagement, max tracking delta 1.32cm, 0 spikes everywhere). New wiki page: `wiki/concepts/physics-plausibility.md`. Full results: `wiki/experiments/phasic-v2-M4-gate.md`. Increment 2 (CoM support-polygon check, plan.md T4.2) NOT built -- deferred pending direction. Full build/debug trail: `planLog.md`.

## [2026-07-10] design | phasic-v2 M4/T4.2 CoM check built, then DISABLED BY DEFAULT (follow-up to same-day M4/T4.1 entry): --enable-com now defaults False in scripts/physics_plausibility_pass.py -- code unchanged/kept/tested, only the CLI default flipped. Decision: a purely kinematic CoM/support-polygon estimate can't distinguish genuine static-balance failure from a dynamic posture leaning on momentum -- measured ~40cm "violations" on luigi_standSupine_08's lying-to-standing transition that are real, deliberate motion, not defects. Revisit once physics-aware training provides actual dynamics data (mass/inertia/contact forces), not this kinematic pass. 4 real bugs found+fixed while building it before reaching this decision: (1) single-support false positives (105/122 "violations" on standup_01 were normal locomotion, fixed via double-support-only restriction), (2) independent-QP vel/accel reintroduction (root accel -577.7 vs +-10.0 m/s^2, fixed via ONE combined QP sharing vel/accel hard rows with CoM soft-slack rows), (3) solver-tolerance false positive (1/dt^2=14400x amplification of OSQP's own eps_abs, fixed via a documented ACC_CHECK_TOL=1e-2), (4) large-violation over-correction (the 40cm luigi_standSupine_08 case, ridge-swept 1000x with no effect confirming it wasn't a tuning issue, fixed via --com-max-correction=8cm cap -- beyond it, flag don't correct). retargetingPipeline.sh needed NO change (Stage 4.6 never passed --enable-com, so the new default applies automatically). Full story: planLog.md M4/T4.2, wiki/concepts/physics-plausibility.md (new Increment 2 section), wiki/experiments/phasic-v2-M4-gate.md (updated). M4 now fully done.

## [2026-07-10] feat | phasic-v2 M5 done: per-limb cleanup solver, plan.md's core new deliverable (P4 phase). New `scripts/refine_limbs_contactfirst.py`, opt-in Stage 4.7 (LIMB_REFINE=on/off, default off, verified byte-identical no-op via two fresh consecutive off-runs -- an earlier stale-comparison false alarm was investigated and ruled out). Root frozen throughout (qpos[:,0:7] unchanged every frame/round) -- every remaining DOF is a plain hinge joint, no quaternion retraction needed anywhere, simpler than physics_plausibility_pass.py. 4 limb chains (legs 6-DOF, arms 7-DOF), Gauss-Seidel (legs first), each a whole-clip banded QP: posture ridge + smoothness + XY/Z-split Cartesian tracking ridge (XY boosted on planted frames, Z never boosted so a floor-fix is never fought -- same pattern as Stage 4's on-floor rows) + floor/self-collision/swing-clearance inequality rows. 5 real bugs found and fixed during verification (full detail in planLog.md): (1) plan.md's "2 rounds" insufficient, bumped default to 10; (2) tracking metric compared against the wrong reference (original human target instead of the input's own achieved position), fixed to match physics_plausibility_pass.py's convention; (3) floor penetration on TORSO_LINK (outside any limb chain) misread as oscillation until split into LIMB (fixable, gated) vs CORE (architecturally out of scope, reported only) via a new _limb_body_ids() body-ancestry classifier; (4) planted frames could slip up to ~20cm from nearby swing corrections bleeding through the smoothness coupling -- fixed via an XY/Z-split Cartesian ridge (XY boosted on planted frames, matching Stage 4's floor-row pattern) PLUS adding plant-slip as an explicit 2nd-priority lexicographic score term (the same "foot_floor_err... essential once floor rows exist" lesson Stage 4's own SCA loop already learned); (5) the Gauss-Seidel loop diverged on several clips (pen+self growing monotonically over 10 rounds) -- tried and rejected two fixes (fixed-reference regularization regressed a working clip; reset-to-best-on-first-failure regressed a different working clip) before landing on PATIENCE=2 (allow up to 2 consecutive failures before resetting to the last known-good state), verified to fix all three failure modes together. Full 20-clip corpus (ok=20/20, 0 spikes, root frozen exactly): 8/20 clean pass, 5/20 near-miss, 7/20 severe whole-body-lying-phase cases confirmed (via direct body-contact-count inspection) to be architecturally out of scope for a root-frozen solver -- exactly the risk plan.md's own Risks section anticipated ("root frozen => reach saturation"), all safely protected by keep-best (never worse than input). Cross-checking selfpen against the M4 baseline found a 6th, subtler cost: on 4 clips keep-best accepted a self-collision increase in exchange for floor-pen/slip gains elsewhere (correct per the score's stated priority, but a real trade-off not visible from the pass's own printed metrics). Consequence: M5 does NOT replace the 2 Luigi per-clip floor flags (luigi_standSupine_08 is one of the 7 unfixable cases) -- ships as an additional optional layer, same pattern as physics-plausibility, layered after it in the pipeline. Full results: `wiki/experiments/phasic-v2-M5-gate.md`. New wiki page: `wiki/concepts/limb-cleanup.md`. Full build/debug trail: `planLog.md`.

## [2026-07-10] feat | phasic-v2 M6 done, ALL MILESTONES (M0-M6) NOW COMPLETE on branch phasic-v2. Scoped M6 efficiently given cumulative session time already spent on M1-M5's real bug-hunting: T6.1 (Stage 4.5 invariant check) documented not built -- ground_shift legitimately varies per clip (same M3 finding: Alex's own qpos frame is a separate coordinate space from the canonical-human invariant), the operative check is eval_artifacts_corpus.py's grnd_pen_plant columns, already the basis for every M1-M5 gate. T6.2 (corpus vs M0) reused wiki/experiments/phasic-v2-M2-T2.2-gate.md as the current shipped-default state (unchanged since that gate, no new run needed) -- visual render spot-check explicitly SKIPPED given time budget, flagged not silently dropped. T6.3 (ONE config) assessed honestly: 2/20 clips carry ONE minimal justified flag each (down from 5-9 pre-redesign), the correct final state not a shortfall. T6.4 (ablation): PHYSICS_PASS/LIMB_REFINE (already added at M4/M5) ARE the ablation knobs for the two genuinely-optional new phases; P0-P2 are foundational, main branch/M0 baseline is that comparison point. T6.5: new wiki/concepts/phasic-architecture.md (phase map P0-P5, contracts, settled decisions, ONE-config status, ablation table, verification methodology used throughout) -- the entry point for future sessions. Updated wiki/index.md, pipeline.md, grounding.md, contact-first-ik.md with pointer notes. Full plan.md (M0-M6, repo root) and planLog.md (complete build/debug trail, every bug found + every rejected fix attempt across all 6 milestones) are the permanent record. Session summary: 4 new scripts (contact_labels.py, ground_canonical_human.py, physics_plausibility_pass.py, refine_limbs_contactfirst.py), 1 new pipeline stage always-on (2.5) + 2 new opt-in stages (4.6, 4.7), the two Luigi clips' per-clip flag count cut from 5-9 each to 1 each, corpus-wide 0 hard joint violations and 0 velocity spikes maintained throughout every milestone. Next: SESSION_HANDOFF.md update (per explicit user instruction) for the returning session tonight.

## [2026-07-11] design | Hierarchical cascade v1 planned; phasic-v2 verdict recorded; plan.md rewritten
Prabin reviewed phasic-v2 results (architecture win, not decisive metric win — Luigi clips flat vs main's hacks, 7/20 lying clips out of root-frozen reach) and proposed an HQP-NLP cascade (`hierarchical.md`). Claude's review appended there as "Claude's comment" with settled rulings: CoM tier DROPPED (M4's 40cm lesson); zero-slip contacts upgraded to a design GOAL (Prabin: robots must not slip — supersedes "a cm of slip is learnable" for active contacts); null-space HQP rejected — with CoM gone the hierarchy reduces to ONE QP per frame (hard constraints: contact pins + floor + joint limits; soft objective: tracking); stay MuJoCo/OSQP position-level; torque limits blocked on missing actuator specs. plan.md rewritten: phasic-v2 plan compressed to a Background section, new H0–H4 milestones for the executing model — H1 root-z probe in refine_limbs_contactfirst.py round 2 (the 7 lying clips) FIRST, then H2 hard-tier Stage 3 (`S3_HARD_TIER`), H3 conditional Stage-4 hard anchors, H4 corpus gate + defaults decision. Sliding-contact clips (standup_slideHandsBack_03) will deliberately deviate from human — needs visual sign-off at H4.

## [2026-07-11] test | Hierarchical-v1 H1 done: root-Z DOF probe in refine_limbs_contactfirst.py — SAFE but NO benefit, reported per plan.md gate
Added `--root-z` (default off, byte-identical no-op verified) to `scripts/refine_limbs_contactfirst.py`: a 5th 1-DOF pseudo-limb (qpos index 2 only, root x/y/orientation stay hard-frozen), reusing `_solve_limb_qp` verbatim, targeting the 7 whole-body-lying clips M5 couldn't reach (plan.md's own named fallback). Ran standalone on all 7 target clips against real M2/M4 pipeline output. Result: root-z genuinely engages every round (nonzero `root_z_floor_rows` in the log) but NEVER wins keep-best on any of the 7 — `Root-Z delta: max=0.00cm` every time, `floor_pen(core)` bit-for-bit identical warm-vs-final on all 7. Self-collision jumps sharply the moment root-z-touched rounds enter the mix, losing lexicographically even where core-pen improved transiently. Safe (0 spikes, root x/y/quat exactly frozen, keep-best never regresses) but zero net value as tuned — root cause (Gauss-Seidel ordering entangling root-z's effect with the same round's limb corrections) not isolated; untried alternatives (round-0 activation, solve-order swap, looser self-collision penalty for root-z rows) flagged, not attempted. Per plan.md's explicit instruction, reported to Prabin at this gate rather than proceeding to H2 or tuning further unprompted. Full numbers: `planLog.md` H1; wiki: `wiki/concepts/limb-cleanup.md` new "Root-Z probe" section.

## [2026-07-11] test | Hierarchical-v1 H2 done: Stage-3 hard-tier contact/floor probe — reactivated a retired mechanism narrower, found a NEW confirmed dead end, zero net benefit on the corpus gate
Added `--hard-tier`/`--floor-hard` to `scripts/solve_fbx_canonical_alex_contactfirst.py` per Prabin's "move to H2" instruction. Discovered before writing code that Stage 3's `--hierarchical` two-level solve already exists (dormant, never used by the pipeline) and was already tried+retired (`wiki/experiments/retired-approaches.md`: hand palm-pin forced hard starved body tracking on pivoting get-ups). Flagged the conflict with the settled "don't re-litigate" decision to Prabin before writing more code; he chose the narrower path (feet+floor hard, hands untouched — the retired note blamed hands specifically). Verified `--hard-tier` alone (feet-only) is SAFE on `standup_natural_01` (the clip the original regression named). Found `--floor-hard` (floor-as-hard-tier) is a NEW, independently confirmed dead end: a 44-METRE divergence on the same clip, isolated via direct A/B against `--hard-tier` alone. Decoupled the two flags so this can't ship combined; `--floor-hard` stays in the codebase, off, documented broken. Ran the real 20-clip corpus (`S3_HARD_TIER=on`, fresh output dirs, `ok=20/20`) against the frozen baseline: `ftSlip`/`coll%`/`selfPen` are BIT-IDENTICAL on every clip (Stage 4's contact QP already delivers zero-slip regardless of Stage-3's mechanism — H2 re-confirms rather than overturns the original retirement's own reasoning), `plPen` net slightly worse (mean +0.60cm, one clip +8.72cm). Also found via a new Stage-3-level diagnostic that even the safe form doesn't cleanly self-converge to zero slip internally on several clips (hold_slip up to 41cm before Stage 4 cleans it up). Verdict: H2 does not clear the bar to ship — reported to Prabin rather than proceeding to H3 (explicitly conditional on H2 improvement) unprompted. Full trail: `planLog.md` H2; gate: `wiki/experiments/hierarchical-v1-H2-gate.md`; wiki: `wiki/experiments/retired-approaches.md` (new floor-hard entry), `wiki/concepts/contact-first-ik.md` (new section).

## [2026-07-11] close | Hierarchical-v1 CLOSED after H2 (Prabin's call) — pipeline back to phasic-v2 defaults
Per Prabin's instruction, closed `plan.md` ("Hierarchical cascade v1") after H2's zero-benefit result — H3/H4 never started (H3 was explicitly conditional on H2 showing improvement). Marked `plan.md`'s header CLOSED with a pointer to the H1/H2 verdicts. Cleaned up the H2 experimental output directories (`outputs/{contactfirst,global_opt_contactfirst,grounded_contactfirst,ihmcJsons-native120hz,ihmcJsons50hz}_h2`, gitignored/local scratch — gate numbers already captured in `wiki/experiments/hierarchical-v1-H2-gate.md`/`.csv`, nothing lost). Verified `retargetingPipeline.sh`'s diff is fully additive (all new env knobs default off/unchanged) — current tree is phasic-v2's shipped defaults plus dormant opt-in flags (`--hard-tier`, `--floor-hard`, `--root-z`), same state as the M2/T2.2 gate. `--root-z`/`--hard-tier`/`--floor-hard` all stay in the codebase (not deleted) per this project's retired-approaches convention. `SESSION_HANDOFF.md` updated for next session.

## [2026-07-13] test | Swing-foot toe-clearance de-pitch (`--swing-clear`) — REJECTED on visual review; diagnosed get-up foot dig as a knee-140° embodiment limit
Investigated the get-up-class swing-foot floor penetration (rigid ~20cm foot plate toe digs ~10–18cm through the floor copying the human's plantarflexed low-step). Built `--swing-clear` in `scripts/solve_fbx_canonical_alex_contactfirst.py` (default off): (a) cap the toe-down pitch of a swing foot's orientation target, (b) paired hip/knee temporal-continuity `posture_reg` boost to stop the redundant-leg IK branch-flipping (Prabin's diagnosis — per-frame IK flips a branch Stage 4 can't smooth; the fix is anti-flip regularization AT the per-frame solve, verified: aggressive cap alone → 43°/frame spike survives Stage 4, cap+continuity → 0 spikes), (c) self-collision weight boost to stop the continuity reg jamming thigh↔torso. Corpus (`SWING_CLEAR=on`, 20 clips): `anyPen` mean 9.0→5.6cm, 0 spikes, 0 JLvi, self-pen depth flat (0.85→0.91cm) — looked like a win. **REJECTED after rendering frames**: on `luigi_standProne_03` the aggressive de-pitch CONTORTS the leg (flattening a foot tucked UNDER the body forces a grotesque knee/ankle pose — smooth in joint-velocity, ugly in pose; scalar smoothness metrics missed it, had to look at pixels). Key insight: flattening a *tucked* foot is intrinsically ugly regardless of stage, so the mooted GlobalOPT version (Probe B) would fail the same way — kinematics, not smoothness. Separately diagnosed `luigi_standSupine_08`'s persistent ~14cm `anyPen` (constant 13.8→14.1 across EVERY version, never reduced) as the SAME embodiment limit: `RIGHT_KNEE_Y` pinned at its 140° max the whole supine→stand transition, so the foot can't tuck as tight as the human and the heel corner drives to −14cm; ankle target is correct (+8cm), achieved sags 10cm below with knee saturated. NOT registration/swing/tracking — a hardware joint-range gap, same class as the 7 whole-body-lying clips. Documented both in `wiki/results/tradeoffs-limits.md` (embodiment limit) + `wiki/experiments/retired-approaches.md` (swing-clear). Code kept, all flags default off, shipped pipeline byte-unchanged. Full knobs/sweep: `planLog.md` swing-clear.

## [2026-07-13] test | Root-cause split (Stage 3 vs Stage 4) + working fix (`--sens-foot-min-pen`) for the Stage-4-introduced half of the swing-foot dig
Prabin proposed "coordinated whole-foot re-placement, let torso Z move if needed" as the real fix (superseding `--swing-clear`). Built `--leg-floor-refine` in `scripts/solve_fbx_canonical_alex_contactfirst.py` (Stage 3): synthesizes a temporary PLANT for a penetrating leg (ankle blended to floor+clearance, foot-flat align turned on) AND relaxes pelvis/torso position tracking so the root is free to rise/shift. Ran on `luigi_standProne_03` — detected ZERO penetration in Stage 3's OWN raw output. This forced a re-diagnosis: **measured penetration at each stage directly** (fr320, ungrounded Alex frame vs `alex_floor_z`): Stage 3 raw ≈1.5cm (borderline), Stage 4 pre-grounding ≈9cm (**Stage 4 ADDS ~7.5cm**). Confirmed on 2 more clips (`luigi_standSupine_08` +2.6cm, `standup_side_05` +3.1cm) — Stage 4 always adds penetration, never removes it, but the MAGNITUDE differs: `standProne`'s dig is predominantly Stage-4-introduced (Stage 3 nearly fine); `standSupine`'s knee-140° dig (see prior entry) is predominantly Stage-3-originating (Stage 3 itself already at −17.8cm). Two genuinely different mechanisms under the same `anyPen` metric — explains why `--swing-clear` (a Stage-3 fix) had to fight so hard for `standProne` (compensating in advance for damage Stage 4 hadn't done yet) and why `--leg-floor-refine` found nothing to fix there.

Traced the Stage-4 mechanism: isolated Stage A (closed-form tridiagonal smoothing, `--n-outer 0`) alone reproduces (and slightly exceeds) the full degradation — Stage A's floor-blind smoothing is the culprit, not Stage B's QP (Stage B actually partially recovers ~5cm on top of Stage A's worse result). Found an EXISTING, already-built protection mechanism for exactly this (`_detect_floor_sensitive_frames` / `lambda_track_frames`, added in an earlier session, its own docstring literally analyzes this same clip) — but its protection weight was **exactly 0.0 across the entire dig window** despite being active elsewhere in the clip (113/802 frames protected, none in [290,335]). Root cause: it detects violation via MuJoCo mesh-CONTACT depth (`ct.dist`), which reads shallower (~1.1cm) than the sole-corner-SITE depth (~1.5cm) this codebase's other floor checks use — so the swing foot's dig never crosses the function's 1.5cm `min_pen` default.

Lowering `min_pen` globally "fixed" the window but caused a NEW regression: a 26°/frame WRIST_X jump at an unrelated frame (183-193, hand-push-phase) — the widened protection zone touched joints having nothing to do with the leg. Root cause: `lambda_track_frames`'s per-frame boost was applied UNIFORMLY across all joint columns (except a small hand-roll exclusion list), so ANY newly-flagged frame boosts tracking on EVERY joint, not just the relevant one. Fixed properly: added `body_filter` param to `_detect_floor_sensitive_frames` (restrict violation detection to leg/foot bodies) + a genuinely per-JOINT-and-frame `lambda_track_frames` matrix (the 2D capability already existed in `stage_a`'s docstring, just wasn't being used this way) — the foot-scoped lower threshold now boosts ONLY the 12 leg/ankle joint columns at flagged frames, never wrist/shoulder/spine. New flags: `--sens-min-pen` (existing threshold, now exposed), `--sens-foot-min-pen` (new, second foot-scoped pass, default = `--sens-min-pen` = exact no-op).

**Result on `luigi_standProne_03`** (`SENS_FOOT_MIN_PEN=0.008`, real pipeline): `anyPen` 10.1→**3.2cm**, 0 velocity spikes, 0 frames >0.2rad, max jump moved to `LEFT_KNEE_Y` (10.3°, not wrist). **Rendered frames at fr300/310/320/325/330 — clean natural crouch, no contortion** (unlike `--swing-clear`). Saved: `outputs/renders/sensfoot_luigi/luigi_standProne_03_sensfoot.mp4`.

**Corpus validation — first attempt confounded** (reused stale Stage-3 npz contaminated by the earlier rejected `--swing-clear` corpus run for 19/20 clips, since only Stage-4 outputs were cleared; caught before drawing conclusions, exactly the stale-cache class of bug this project's own history warns about repeatedly). **Redone clean** (fresh Stage-3+4 for all 20, `SWING_CLEAR` off): **18/20 clips byte-identical to baseline** — the mechanism is gated on `--floor-collision on`, which only the two Luigi clips currently have per-clip, so it's correctly inert everywhere else. `luigi_standProne_03`: clean win (anyPen 10.1→3.2, plPen/coll%/selfPen ≈flat). `luigi_standSupine_08`: MIXED (anyPen 14.0→10.7 but coll% 16.2→30.2, plPen 0.8→3.0) — confirms the diagnosis: this fix targets the Stage-4-introduced mechanism, and `standSupine`'s dig is the OTHER (Stage-3/knee-140) mechanism, so tightening Stage-A tracking there partially helps the number but pushes the cost into self-collision instead (nowhere safe for the knee-saturated leg to go). **0/20 spikes, 0 JLvi, corpus max jump 10.3°.**

**Status: real, working, narrow fix. Default off (`SENS_MIN_PEN`/`SENS_FOOT_MIN_PEN` = 0.015 = inert).** Recommended: enable for `luigi_standProne_03` specifically (per-clip). Do NOT expect it to help `standSupine`-class (knee-range) clips — different mechanism, see `wiki/results/tradeoffs-limits.md`. `--leg-floor-refine` (Stage 3) stays in the code, harmless/no-op by default, not the lever for THIS class but not proven wrong either — untested standalone on a genuinely Stage-3-originating case now that the two mechanisms are properly separated.

**Session-level conclusion (Prabin's, confirmed by every mechanism tried this session)**: the swing-foot/tuck-foot floor penetration is not a solvable-by-formulation problem in general — `--swing-clear`'s contortion and `standSupine`'s knee-140° saturation are both real morphological/joint-range gaps between Alex and the human demonstrator, not bugs in the retargeting math. Faithful human-copying hits a wall the pipeline cannot tune around. Direction going forward: treat human motion as a reference/inspiration rather than a literal target where Alex's kinematics diverge from the human's, rather than continuing to chase closer fidelity via solver tuning.
